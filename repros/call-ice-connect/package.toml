# Call engine Stage 2b (ICE connectivity) — wasi:cli/command wandrpkg.
# Run: wandr-host --run-once wandr.probe.ice
app_id      = "wandr.probe.ice"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
