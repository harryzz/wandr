# Call engine Stage 3 (SDP signaling) — wasi:cli/command wandrpkg.
# Run: wandr-host --run-once wandr.probe.sdp
app_id      = "wandr.probe.sdp"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
