# Call engine Stage 3 (SDP signaling) — wasi:cli/command warpkg.
# Run: wart-host --run-once war.probe.sdp
app_id      = "war.probe.sdp"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
