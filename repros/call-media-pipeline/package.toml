# Call engine Stage 1 (media plane) — wasi:cli/command wandrpkg.
# Run: wandr-host --run-once wandr.probe.callmedia
app_id      = "wandr.probe.callmedia"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
