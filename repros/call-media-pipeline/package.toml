# Call engine Stage 1 (media plane) — wasi:cli/command warpkg.
# Run: wart-host --run-once war.probe.callmedia
app_id      = "war.probe.callmedia"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
