# wandr-call real-UDP device check — wasi:cli/command wandrpkg.
# Run: wandr-host --run-once wandr.probe.calludp
app_id      = "wandr.probe.calludp"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
