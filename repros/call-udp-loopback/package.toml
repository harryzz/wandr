# wart-call real-UDP device check — wasi:cli/command warpkg.
# Run: wart-host --run-once war.probe.calludp
app_id      = "war.probe.calludp"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
