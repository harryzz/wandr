[package]
name = "wasi-udp-probe"
version = "0.1.0"
edition = "2021"
publish = false

# wasi:cli/command guest (wasm32-wasip2) that de-risks UDP transport for the
# WebRTC call engine: does a guest get working UDP send/recv through wart-host's
# wasi:sockets wiring? (1) localhost loopback proves the wasi-sockets UDP API;
# (2) a best-effort STUN binding request to a public server proves outbound
# internet UDP + yields our server-reflexive address (the srflx ICE candidate).
# No deps — hand-rolled STUN, std::net::UdpSocket only.

[dependencies]
