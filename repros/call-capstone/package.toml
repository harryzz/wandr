# Call engine CAPSTONE — full end-to-end secure call. wasi:cli/command warpkg.
# Run: wart-host --run-once war.probe.call
app_id      = "war.probe.call"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
