# Call engine CAPSTONE — full end-to-end secure call. wasi:cli/command wandrpkg.
# Run: wandr-host --run-once wandr.probe.call
app_id      = "wandr.probe.call"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
