# Call engine Stage 2 (DTLS-SRTP handshake) — wasi:cli/command warpkg.
# Run: wart-host --run-once war.probe.dtls
app_id      = "war.probe.dtls"
version     = "0.1.0"
world       = "wasi:cli/command"
kind        = "system"
composition = "same-store"

[components]
probe = "components/probe.wasm"
