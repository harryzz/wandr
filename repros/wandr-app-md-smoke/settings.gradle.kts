rootProject.name = "wandr-app-md-smoke"

// Standalone build — no skiko / compose deps. The smoke is a *consumer*
// of the markdown system component, validating the task-36 cross-app
// dep wiring end-to-end.
