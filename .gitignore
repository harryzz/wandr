# Each of these subdirs is a separate git repo (or vendored sibling project)
# and is NOT tracked from this top-level. They have their own .gitignore files.
compose-*/
skiko/
wart-*/
# System components (runtime-bundled .wasm packages — see
# tasks/36-cross-app-deps.md). One repo per component; pattern follows
# wart-*/.
markdown-renderer/

# Smoke consumers — task-36 step-7 follow-ups. Own repos for symmetry
# with wart-app-md-smoke/.
md-smoke-rust/

# Second system component (task 40). Same sibling-repo pattern as
# markdown-renderer/.
emoji-picker/

# Task 30 — external sources pulled in for the diagnosis chain. Heavy
# (kotlin-src ~GBs, wasmtime-src ~MBs); kt-memalloc-repro has its own
# Codeberg origin.
kotlin-src/
wasmtime-src/
kt-memalloc-repro/
