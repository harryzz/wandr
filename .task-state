TASK=96
STEP=verify-done
STATUS=complete
LAST_SUCCESS=Task 96 (churn-free shim-first --no-art bringup) DONE+device-verified earlier. THEN this session, follow-on fixes (all device-verified): (1) async sensor enable off the arbiter_lock (sensor_driver worker thread) — removed the 5s input freeze on power-key/proximity; (2) wart-hal-lights crate (AIDL ILights, BrightnessMode SENSOR) + apply_backlight via HAL with sysfs fallback — NOTE device is HIDL @2.0::ILight not AIDL, so falls back to sysfs (works); Effect::SetBacklight now carries sensor:bool (auto=SENSOR/manual=USER); (3) AUTO-BRIGHTNESS 5s FIX: root = platform enableSensor pays a ~5s timeout re-activating a COLD sensor (SLPI powers it down with no clients: "TMG4903 no reporters => ams_deviceInit"); fix = keep ALS warm 120s after screen-off (ALS_WARM_GRACE_MS) — decoupled light_should_be_warm() (enable) from auto_brightness_active() (apply). VERIFIED: quick screen off->on no re-enable, brightness instant (was 5s).
NOTES=Timing proof: queue()=233us, enableSensor()=5.017s (platform libsensorserviceaidl, can't patch). PROXIMITY-in-call has the SAME 5s cold-enable root, NOT yet fixed — same keep-warm pattern applies (pre-warm on incoming ring audio-ring-start + grace before release; sensors module acquire/release ref-count in wart-arbiter-sensors). NEXT: proximity keep-warm + commit all (wart-hal-lights, sensor_driver async, SetBacklight sensor field, ALS warm grace). Builds clean, 27 power tests pass. Device on keep-warm arbiter (5334). 50%-CPU was a user mis-read (not real). Lights HAL: HIDL shim deferred (sysfs works).
