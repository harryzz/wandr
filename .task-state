TASK=117
STEP=6-verify-done
STATUS=complete
LAST_SUCCESS=Task 117 step 6 verified OK — plain `cargo build` is now fully self-contained (no VPX_* env, no pre-build step, no system media library). Own Apache-2.0 crates/wandr-vpx-sys replaced MPL-2.0 env-libvpx-sys; its build.rs compiles vendor/libvpx into OUT_DIR, or uses VPX_LIB_DIR (the Windows/vcpkg path). Verified with all VPX_* unset: 5/5 crate tests, host release links no libav*/libvpx.so, selfview 57.6/60 fps, camera->VP8->decode 60/60/60 PASS.
NOTES=Owning the -sys crate also removed the untested Windows hazard: it emits `static=vpx` which matches vcpkg's vpx.lib directly, so the vpx.lib->libvpx.lib alias is gone from CI + build-host-windows.bat. Deleted scripts/build-libvpx.sh + libvpx-env.sh (build.rs owns the configure flags now — a shell copy would drift). REMAINING: real Signal desktop video call (needs a partner); macOS/Windows CI legs unverified locally; tasks/117 + tasks/STATUS.md still say PROPOSAL.
