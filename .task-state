TASK=90
STEP=M3-done
STATUS=complete
LAST_SUCCESS=Task 90 M3 (saved-network store + auto-connect = WifiConfigManager) DONE + device-verified --no-art. Storage decision: wandr-owned JSON (/data/local/tmp/wandr-wifi-networks.json, 0600, b64'd ssid/psk; keystore2-wrap = future). wandr-arbiter-net is now the WifiConfigManager: verbs wifi-saved-{list,add,update,remove,auto-connect,creds} + wifi-auto-network (pure module + injected store path, 6 unit tests). Bin: NetModule::with_store + wifi-connect-saved handler (module resolves id->creds, bin relays connect to wandr-net.sock). wandr-net bring_up now prefers arbiter wifi-auto-network, falls back to WifiConfigStore.xml. Host: un-stubbed list-saved/add/update/remove/set-auto-connect/connect(id) forwarding to arbiter; disconnect/forget-current still stubbed (need a daemon disconnect verb). Device-verified via CLI: add/list/auto-network/toggle/creds/update/remove all correct; JSON persisted 0600; b64 round-trips space/comma/quote PSK; not-found errors. Skipped live connect(id)+auto-join restart (would trigger associations) - both ride M2-verified paths + the resolve/read paths are CLI-verified.
NOTES=NOT committed yet. Remaining task-90: M4 (wandr.settings.wifi picker app consuming wifi-settings + manual static-IP). Follow-up: daemon disconnect verb to un-stub disconnect/forget-current. All 3 layers build+deploy; --wandr-only restart done, link up.
