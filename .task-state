TASK=100
STEP=M4-interactive-verify
STATUS=in-progress
LAST_SUCCESS=Task 100 M4 RENDERS on device — first Slint UI on wandr (screenshot-verified, ~0.7% idle CPU)
NOTES=wandr.slint.test installed + launched on the new-verbs host (stack restarted --wandr-only). Rendered correctly: parley text via draw-glyphs (Inter embedded by i-slint-common on wasm), fluent Button/LineEdit/Slider/CheckBox, linear gradient, drop shadow, ListView rows. Pending: USER interactive verification (tap +1 counter/animation, type in LineEdit incl IME, drag-scroll list, slider). Then M5 decision.
