TASK=23
STEP=mvp-verify-done
STATUS=complete
LAST_SUCCESS=Task 23 MVP verified — ResourceLimiter + Store::call_hook + frame_tick firing live on Pixel 2 XL. Cold start logged 14 memory.grow events (1->2->4->8...->512 pages, ~32MB), then steady state. Host-call rate = 10/frame at 60fps after Compose boot.
NOTES=GuestProfiler sampling intentionally deferred: requires Config::epoch_interruption(true) which changes AOT-cwasm contract and breaks loading the existing cwasm. Fix is a separate iteration that rebuilds the cwasm with matching config (or ships a parallel profile-build cwasm). The ResourceLimiter trio shipped here is what actually characterizes the ProgressIndicator leak — primary motivation served. Profile-feature .so is identical to default-feature in size (cargo strips dead code well); APK md5-verified to contain `wart-profile` strings.
