TASK=88
STEP=M2-cold-chip-power-up-DONE
STATUS=in-progress
LAST_SUCCESS=M2 cold chip power-up via IWifi HAL DONE+device-verified. wart-hal-net::wifi_chip drives IWifi over binder as uid system: isStarted()→start()(poll)→getChip→configureChip(STA mode derived from concurrency combos)→createStaIface. Idempotent (early-return if STA iface exists). Daemon runs su 1000 --power-chip in no-carrier branch before supplicant. Verified: --stop-chip (isStarted false) → daemon → IWifi power-up → associate(ISupplicant) → DHCP → INetd/IDnsResolver → ping 0% + codeberg HTTPS 200, zero AVC. hal-net 6 tests pass.
NOTES=Source-grounded (read IWifi/IWifiChip/IWifiStaIface AIDL + ChipMode nested structs + IfaceConcurrencyType before patching; compiled first try). DEVICE QUIRK: wlan0 netdev is kernel-driver-created + PERSISTENT (survives IWifi.stop + removeStaIface) — so cold signal is isStarted(), NOT iface_exists (removed that wrong gate). Full --no-art WiFi stack now Android-native: IWifi power-up + ISupplicant associate + pure-Rust DHCP + INetd routing + IDnsResolver DNS, all as uid system. M2 remaining: scan/known-net selection. M1 open: war:connectivity host wiring + test guest; restore-art teardown cleanup. NOT committed.
