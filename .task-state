TASK=114
STEP=P2.3e-gradients-clip-images-device-verified
STATUS=in-progress
LAST_SUCCESS=Task 114 P2.3d/e DONE (device-verified Pixel 2 XL): CGContext now has gradients (CGGradient + drawLinearGradient/drawRadialGradient(in:) via graphics.linear/radial-gradient shaders), clipping (clip(to:)->clip-rect, clip()->clip-path), and images (makeImage(rgba:)->graphics.image-from-rgba8, draw(_:in:)->draw-image-rect; CGImage holds wasi own_image, drops on deinit). Scene shows linear gradient rect + radial gradient clipped to triangle + RGBA bitmap, alongside dash/shadow/mask-clip — all via OpenCoreGraphics CGContext->wasi:canvas. CGContext also carries the graphics factory (from embedding canvas-context.graphics()). GOTCHAS: (1) makeImage must keep `ret` LOCAL to the withUnsafeMutableBufferPointer closure (nested-inout writeback); (2) image draws need opaque-WHITE paint color — host multiplies color.alpha × paint.alpha (feedback_paint_alpha_pipeline) so color=0 => transparent image.
NOTES=Remaining P2.3: TEXT — needs importing wasi:canvas/text (glyphs + layout) into the spike world + a typeface (typeface.from-bytes; host preopens /system-fonts). Heaviest piece. Component ~60MB (Foundation). Deploy loop unchanged. SwiftUI (OpenRenderBox+OpenSwiftUI) out of scope.
