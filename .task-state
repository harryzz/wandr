TASK=75-ringrtc-signal-calls
STEP=phase3-real-identity-keys
STATUS=in-progress
LAST_SUCCESS=Phase 3 core (real identity keys) + big green/red handset toggle DONE + device-verified (handset turns red on tap = call placed; user-confirmed). Reactive fix: CallBar/ThreadHeader now take call-state as PROPS from app() (were memoized → never re-rendered). wart-call random ICE creds (14/14). engine uses real serialized identity public keys for SRTP HKDF (store.identity + get_identity; self-call keys with own id). Committed through b94c3ac8 + the UI refresh fix now.
NOTES=NEXT: real-Signal-client AUDIO interop — needs the user's real Signal app + iteration (the actual connect+two-way audio with a non-wart client; expect to shake out RTP ext ids / param fidelity like repros/call-browser did vs libwebrtc). Also still unverified live: duplex audio MMAP -889 (open_capture+create_track same device), NAT/TURN (LAN-only host candidate). Carry-over UX: no call timeout (unanswered rings forever). My adb taps don't hit the top header strip (user's finger does) — verify UI changes WITH the user.
