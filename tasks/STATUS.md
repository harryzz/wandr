# Task status ledger + file index

_Moved out of CLAUDE.md 2026-05-30 to keep it lean. This is the living
task ledger — update the status row when a task changes. Per-task detail
lives in `tasks/NN-*.md`._

## Current status

**Working end-to-end PoC as of 2026-05-15.** Real Compose UIs render on the
Pixel 2 XL (Android 15 / API 35). Tasks 01–14 landed organically while
shipping the full Compose port — not as discrete sequential milestones.

| Task | Description | Status |
|------|-------------|--------|
| 01 | Rust host skeleton, Android cross-compile | ✅ complete |
| 02 | WIT interface + Rust canvas implementation | ✅ complete |
| 03 | Skiko wasmWasi port — basic canvas, text, animation | ✅ complete |
| 04–07 | Compose app, input, coroutines, AOT deploy | ✅ complete |
| 08 | Paint completeness + missing primitives (blendMode, strokeCap/Join, arc, drrect, clip-rrect, skew, matrix concat) | ✅ complete |
| 09 | Path + PathBuilder (SVG serialization) | ✅ complete |
| 10 | TextBlobBuilder — multi-run styled text | ✅ complete |
| 11 | Gradient shaders (linear, radial) | ✅ complete |
| 12 | Image rect drawing | ✅ complete |
| 13 | ColorFilter (tint, invert) | ✅ complete |
| 14 | Paragraph layout — full layout + getRectsForRange / getGlyphPositionAtCoordinate / getWordBoundary | ✅ complete |
| 15 | rsbinder pipeline — AIDL submodule, ProcessState init, codegen wiring | ✅ device-verified 2026-05-17 |
| 16 | Vibrator HAL via IVibrator binder (+ @nullable null-callback workaround) | ✅ device-verified 2026-05-17 — phone buzzes |
| 17 | Lights HAL via ILights binder + new WIT lights interface | ✅ implementation verified; graceful no-op on Pixel 2 XL (no AIDL ILights HAL on this device, Pixel 3+ has it) |
| 19 | IPower + IThermal HALs — performance hints + thermal state | ✅ device-verified 2026-05-17 — 11 sensors, CPU 39°C, throttle NONE |
| 20 | Sensors via ISensorManager (frameworks-layer AIDL) — accel/gyro/proximity/light/etc | ✅ device-verified 2026-05-17 — 29 sensors, accel z=9.61 m/s² |
| 21 | Audio playback via rsbinder to IAAudioService (Path B-AAudio variant) | ✅ device-verified 2026-05-17 — 440 Hz beep audible on Pixel 2 XL speaker |
| 18 | Compose `LocalHapticFeedback` → WIT haptics adapter | ✅ device-verified 2026-05-17 — Material3 button click buzzes Pixel 2 XL |
| 22 | ISurfaceComposer rsbinder round-trip (roadmap §5 de-risk) | ✅ device-verified 2026-05-17 — SurfaceFlinger reachable; transport validated |
| 23 | Profiling hooks (`ResourceLimiter` + `GuestProfiler`) | 🟡 MVP device-verified 2026-05-17 — 3/4 hooks live; GuestProfiler deferred (epoch-interruption cwasm-rebuild needed) |
| 24 | Bisect the WasmGC-heap leak — find the real retention chain (Kotlin/Wasm continuations? kotlinx-coroutines-wasmWasi? Compose Snapshot?) | 🟡 step 1 done 2026-05-17 — minimal reproducer in `wandr-leak-repro/`; leak isolated to Kotlin/Wasm `suspend` codegen (~9 MB/s, OOM in 6:37). Steps 2-4 moot; pending upstream file. **UPDATE 2026-05-21:** root cause re-attributed — NOT Kotlin codegen but **wasmtime DRC having no auto-sweep** (sweep only fires on `memory.grow` failure). Filed upstream as [wasmtime#13403](https://github.com/bytecodealliance/wasmtime/issues/13403). fitzgen's fix PR#13422 (auto-GC trigger when the over-approx-roots list doubles) verified on device 2026-05-21: **fixes the leak** (heap bounded — desktop RSS flat 43 MB vs 4 GB baseline) **but reintroduces an ANR** via GC-frequency overhead — the inline `force_gc` fires very frequently on the Compose guest and each GC's root scan (`trace_vmctx_roots`) blocks the render thread. Not a clean fix as-is. Device reverted to the known-good 2.4.257/wasmtime-44 build. **FIXED 2026-05-30 — adopted wasmtime 45.0.0 (task 65):** the released 45.0.0 carries #13422 + its required follow-up #13450 (`array.copy` stack-map) + an improved grow-vs-collect heuristic, and task-64's on-demand rendering cut the Compose guest from 60→~1 fps idle — together the leak is bounded with NO ANR (desktop repro flat 39 MB; device 60 fps+scroll 3 min flat ~220 MB, smooth). See [[wasmtime-drc-no-autoschedule]] + `tasks/65-wasmtime-45-adoption.md`. |
| 25 | Diagnose the `suspend` state-machine leak: tighten repro, identify leaked structref type, locate kotlinc-wasm-backend codegen pass | 🔲 scoped, step 1 starting |
| 27 | Skiko WIT-shaped gaps: Image.makeFromEncoded, Shader.makeSweepGradient, Image.makeShader, Shader.makeBlend, Gradient-object overloads on linear/radial/sweep | ✅ device-verified 2026-05-18 — smoke card shows all four new APIs ok; Compose `Brush.sweepGradient` round-trips through the new WIT verb. Bitmap.makeShader deferred. |
| 28 | Wire `org.jetbrains.skia.Canvas(bitmap)` to host-side raster surfaces — 38 new bc-* WIT verbs, full 41-stub buildout, Bitmap.surfaceId + Image.makeFromBitmap snapshot bridge | ✅ device-verified 2026-05-19 — SegmentedButton selected segment (checkmark + label) renders; DatePicker renders/swipe/year-pick work. Chevron `< >` previously blocked by Tooltip SIGILL — unblocked 2026-05-19 by task 30 adapter fork. SegmentedButton *unselected* segments rendered as opaque grey fills with no label text (pre-existing, never worked) — root-caused + fixed in task 32. |
| 29 | Diagnose the Material3 TooltipBox SIGILL on wasi — bisect Press/long-press path to `BasicTooltipState.show()` | ✅ resolved 2026-05-19 via task 30 — was not a Tooltip-specific bug; adapter State at linear-mem 0x10008 was corrupted by Kotlin/Wasm's allocator. Self-heal in adapter fork unblocks Tooltip + DatePicker chevrons. See [[wasi-adapter-state-corruption]]. |
| 30 | WASI adapter `assert_fail` + wasmtime signal-handler diagnosis (root cause of task 29) | ✅ resolved 2026-05-19. Originally framed as a Kotlin/Wasm `ScopedMemoryAllocator.destroy()` range-propagation bug — **but JetBrains rejected that framing on [KT-86415](https://youtrack.jetbrains.com/issue/KT-86415/) (2026-05-20):** real cause is a use-after-free — the WASI adapter holds canonical-ABI `realloc` memory across Kotlin's aggressive `freeAllComponentModelReallocAllocatedMemory`. See [[kotlin-wasm-scopedmemory-destroy-bug]] (UPSTREAM CORRECTION section). Our `2.4.255-SNAPSHOT` stdlib patch was an empirical leak-trade stopgap; the adapter fork additionally self-healed corrupted State. **Both superseded by task 34's Option B fixed-partition fix (device-verified 2026-05-21) — the self-heal is now removed.** ✅ fully closed 2026-05-20 — Step 4 (signal-handler) deferred with re-open criteria; Step 6 (related-bug audit) found no additional retroactive fixes (Tooltip SIGILL was the only bug with this root cause). |
| 31 | Animated `graphicsLayer` invisible inside popups — DropdownMenu/AlertDialog/Tooltip enter animations | ✅ resolved 2026-05-20. Root cause: `SkiaGraphicsLayer.requiresLayer()` sets `RenderNode.layerPaint` for any `alpha<1`; the wasi `RenderNode.drawInto` baked `saveLayer(layerPaint)` into the **parent** recording, freezing alpha at record time (≈0) for popups that record once. Fix in `skiko/.../node/RenderNode.wasi.kt`: emit the recorded `saveLayer` only for genuine filters (colorFilter/imageFilter/non-SrcOver blend); plain alpha rides the live `WasiDrawable` attr. Device-verified — DropdownMenu animates with no `LocalInspectionMode` workaround. See [[popup-overlay]]. |
| 32 | SegmentedButton unselected segments grey-filled, labels hidden | ✅ resolved 2026-05-20. Root cause: `org.jetbrains.skia.Path.makeCombining` (path boolean ops) was a `null`-returning stub on wasi. `Modifier.border` builds a non-uniform rounded border ring via `Path.op(outer, inner, DIFFERENCE)` → null → ring collapsed to a full fill (grey block over the label). Fix: new `path-combine` WIT verb — host runs `skia_safe::Path::op`, converts the EvenOdd result to a Winding-fill path (`as_winding`, since SVG carries no fill rule), returns SVG; skiko `Path.makeCombining` wired to it. General fix — all path-boolean callers now work. Device-verified. |
| 33 | Boot-model bring-up — run the runtime as a standalone privileged process (no NativeActivity) | 🟢 Steps 1, 3, 4, 5 ✅ device-verified (latest 2026-05-26); Step 2 ✅ functionally (codepath runs without Activity) but the `android-activity` dep is still pulled for the NativeActivity sibling — sub-roadmap for the post-ART display path. 5 ordered steps: (1) standalone-surface spike — non-Activity `su`-process creates a fullscreen `SurfaceControl` from SurfaceFlinger via libgui `SurfaceComposerClient`, EGL one frame [keystone de-risk] — **DONE + integrated into wandr-host: `cpp/sf_probe.cpp` proved the path (solid blue frame), then `cpp/sf_surface.{cpp,bp}` (soong `cc_library_shared` `libsf_surface.so`, built in-tree) became the real shim — `wandr-host --standalone` `dlopen`s it, gets a SurfaceFlinger surface with no Activity, and renders via Skia; device-verified. libgui C++ must be built in-tree (out-of-tree header vendoring is infeasible — AIDL+HIDL codegen fan-out) — see [[project-boot-model-libgui-build]]**; (2) decouple host from NativeActivity/winit-Android — ✅ `src/standalone.rs` runs the full cwasm render loop; (3) input from InputFlinger — ✅ device-verified 2026-05-22 for touch (display-clip + 90°-rotation both fixed — the rotation was an `eglQuerySurface` lie on taimen Adreno, renderer now takes geometry from `ANativeWindow`); ✅ hardware keys 2026-05-26 (`SfInputEvent` extended with `kind=10/11` + `meta_state`, `cpp/sf_surface.cpp` emits AKEYCODE_*, `input::dispatch_android_key` maps to (code-point, key-id) using the same numeric IDs the winit NativeActivity path sends — `adb shell input keyevent KEYCODE_A` types into BasicTextField; needs new `sf_request_focus()` shim export called every ~1 s because activity-backed windows (launcher, last app) steal InputDispatcher focus from non-Activity wandr); (4) launch mechanism + SystemUI coexistence — ✅ device-verified 2026-05-26: `scripts/standalone-launch.sh` preflights / pushes-newer / `am force-stop com.android.systemui` + the resolved-home launcher / installs an EXIT trap that restores both, and runs wandr-host in the foreground; `scripts/standalone-recover.sh` is the idempotent backup; `am force-stop` is non-persistent so a reboot is always a clean recovery. Init.rc / sepolicy domain remain deferred (production-only); (5) lifecycle / minimal arbiter — ✅ device-verified 2026-05-26: `src/lifecycle_standalone.rs` adds (a) `sigaction` for SIGTERM/SIGINT/SIGHUP → atomic flag → render loop breaks → fire `Destroyed` → drain 3 frames → return `Ok`, (b) screen-state watcher thread polls `debug.tracing.screen_state` every 500 ms and fires `Paused`/`Resumed` on Off↔On (Doze treated as Paused), (c) panic hook writes `/data/local/tmp/wandr-host-crash.json`, drained + logged on next launch, removed on clean exit. Cold-start `Created → Started → Resumed` already auto-walks via the bridge. Multi-app arbiter explicitly out of scope. `bash scripts/standalone-launch.sh` is the one-line dev entry point. See `tasks/33-boot-model-bringup.md` + `post-art-roadmap.md` §11. |
| 34 | KT-86415 real fix — adapter-State use-after-free via a fixed linear-memory partition (Option B) | ✅ device-verified 2026-05-21. Static partition shipped: Kotlin stdlib `2.4.258-SNAPSHOT` root `ScopedMemoryAllocator` starts at `RESERVED_BASE=0x20000` (one const in `MemoryAllocation.kt`, `destroy()` left stock — no leak); adapter fork `State::new` places `State` at fixed `STATE_BASE=0x10000` (`[0x10000,0x20000)` reserved). Win 1: DatePicker chevrons + Tooltip long-press exercised — 0 SIGILL / 0 State-corruption. Win 2: idle wasm-linear-memory leak 0.111 MB/s = identical to known-good 2.4.257 baseline (0.114) → no regression; residual is the pre-existing wasmtime-DRC leak [[wasmtime-drc-no-autoschedule]]. Task-30 `State::with` self-heal removed (verified unnecessary). The `2.4.258` stdlib is wired via `~/.gradle/init.d/kt-86415-stdlib-override.gradle.kts`; the override stays until KT-86415 lands upstream. See `tasks/34-kt86415-fixed-partition.md`. |
| 35 | App installer (+ thin loader) — the multi-component package boundary | ✅ device-verified 2026-05-26. Six steps landed: (1) `wandr-host/src/app_loader.rs` (AppRef enum + LoadedApp + AppLoader trait + WandrLoader); (2) `standalone.rs` refactor; (3) `lib.rs` refactor (App.component → App.loaded; App::new + App::new_from_asset_bytes deleted; find_cwasm_on_filesystem → path-candidate list); (4) `wandr-host/src/app_installer.rs` — reads `package.toml`, AOT-precompiles each component via `Engine::precompile_component`, writes spec layout `<root>/<app_id>/<version>/{package.toml, components/, cache/, cache-key.toml}` (cache-key has `wasmtime_version` + `engine_config_hash` over `precompile_compatibility_hash` + per-component sha256s); (5) `AppRef::Installed` self-healing reader in WandrLoader — recomputes hashes, on drift re-precompiles + re-stamps; (6) CLI (`wandr-host --install <wandrpkg>` + `--standalone --app <id>`) + scripts/smoke-wandrpkg.sh fixture + on-device smoke (install → load via Installed → frames render; cache-drift self-heal verified by deleting cwasm). New deps: `sha2`, `toml`. `WANDR_APPS_ROOT` env override skirts `/data/wandr/` sepolicy for smoke. Single-component only — multi-component composition deferred to `tasks/36-cross-app-deps.md`. See `tasks/35-app-install.md`. |
| 36 | Cross-app dependencies + system components — the multi-package boundary | ✅ device-verified 2026-05-26. Steps 1–7 complete; cross-app dep chain runtime-validated end-to-end on Pixel 2 XL. Driver: markdown renderer (system-bundled, library-like, same-Store). Q6 resolved (explicit-required `[package].composition`). Steps 1–6 (library-level wiring): `wit/markdown.wit` (Compose-friendly structured spans record; recursion-flat to fit WIT 0.2), `markdown-renderer/` Rust cdylib component (337 KB, pulldown-cmark backed), manifest schema extension in `app_installer.rs` (`PackageKind { App, System }` + `Composition` + `Dependency` + `[dependencies]` table + `[dependencies_resolved]` cache-key section), resolver that refuses install on missing dep, loader same-Store composition via `wasmtime::component::Linker::instance` (cloned `Guest` accessor; one-dep dispatch by interface string — refactor to registry once N>1), `wandr-app-md-smoke/` Kotlin consumer with hand-written WIT bindings (wit-bindgen 0.53.1 has no Kotlin generator). Step 7 (runtime e2e): new `wandr-host/src/run_once.rs` + `LoadedApp::instantiate_command` + `--run-once <app-id>` CLI dispatch `Command::instantiate` + `wasi_cli_run().call_run` for one-shot `wasi:cli/command` consumers (host-driven, cardinality 1 — same primitive as `renderFrame` with N=1; see new `docs/architecture-host-guest-boundary.md`). The Kotlin smoke validates install/load/linker up to `Command::instantiate` but its `main()` can't run — pre-existing Kotlin/Wasm + WASI-command-adapter throw at module init, confirmed unconditional on-device (wandr-host's `wasi_stderr` makes no difference vs `wasmtime run`). New `md-smoke-rust/` Rust `wasm32-wasip2` consumer sidesteps the Kotlin bug and proves the full call-through-proxy path: `render()` dispatches through `wire_markdown_dep`'s linker closure → dep instance → returns non-empty Document → consumer exits 0. `scripts/smoke-markdown.sh` is the one-line dev pipeline. Visuals v2 device-verified 2026-05-26: full document tree lifted from linear memory by hand-written canonical-ABI code in `wandr-app/src/wasmWasiMain/kotlin/MarkdownImports.kt` (~180 LoC), rendered by `MarkdownCard.kt` as proper Compose widgets per block variant (paragraph + heading + code-block + bullet/ordered list + block-quote with vertical bar + thematic-break) with inline styles via AnnotatedString SpanStyle. Companion bug fix in `markdown-renderer`: `collect_items` was emitting one `SimpleBlock::Paragraph` per inline child for tight-list items → bullets rendered word-per-line; now coalesced into one Paragraph per item. Deferred: `HostState.renderer: Option<SkiaRenderer>` cleanup (--run-once builds a real SF surface, costs ~1s screen flash; revisit if more CLI shapes appear); Kotlin/Wasm command-adapter throw bug (orthogonal — task 37); separate-Store composition mode (wait for a service-shaped dep). See `tasks/36-cross-app-deps.md`. |
| 37 | Kotlin/Wasm + WASI command-adapter init throw — diagnose the unconditional "thrown Wasm exception" from `call_run` | 🔲 scoped 2026-05-26, not started. Confirmed on-device task-36-step-7: Kotlin/Wasm 2.4 + the COMMAND adapter throws at module init unconditionally (empty `fun main() {}` throws identically; wandr-host's `wasi_stderr` to logcat doesn't help; reactor adapter known-good — the bug is command-adapter-specific). Blocks Kotlin CLI consumers but nothing else (Compose apps use reactor; Rust CLI smoke `md-smoke-rust` sidesteps the bug cleanly). 5-step investigation plan in the task doc — start by capturing the wasm-exception payload in `run_once.rs` via `Store::take_pending_exception` + Throwable struct walk (same recipe `lib.rs:354-403` uses for render_frame errors). See `tasks/37-kotlin-wasm-command-adapter-throw.md`. |
| 38 | wandrpkg asset/data files + `my:skiko-gfx/assets.read` host verb | ✅ device-verified 2026-05-26. Bundle `assets/` dir auto-copies to `<install_dir>/assets/`; runtime exposes via new `assets.read(name) -> option<list<u8>>` WIT verb in `skiko-gfx.wit` (implemented in `wandr-host/src/assets_impl.rs` with path-safety guard rejecting `..` + absolute + empty). Parallel WASI fs preopen at `/assets` (read-only) also wired in standalone + run_once — free, ready for a future Kotlin stdlib that grows file APIs (today's stdlib has no fd_read/path_open wrappers). Pivoted mid-impl from WASI-fs-read-in-Kotlin to custom host WIT after confirming Kotlin/Wasm 2.4 stdlib has no file APIs. wandr-app's `MarkdownCard` now reads `assets/demo.md` (1505 bytes) via the new `readAsset()` Kotlin binding; FALLBACK_SOURCE if missing (dev paths / bundles with no assets). Files: `wandr-host/src/{assets_impl,lib,app_loader,standalone,run_once}.rs` (LoadedApp.install_dir + assets_dir() accessor + HostState.assets_dir + preopen + Host impl); `wandr-app/src/wasmWasiMain/kotlin/AssetsImports.kt`; `wandr-app/assets/demo.md`. See `tasks/38-wandrpkg-assets.md`. |
| 39 | Generic dep wiring via wasmtime introspection — replaces hardcoded per-dep `bindgen!` + match dispatch | ✅ device-verified 2026-05-26, co-shipped with task 40. Replaces `wire_markdown_dep` match-dispatch in `wandr-host/src/app_loader.rs` + the per-dep `markdown_bindings` `bindgen!` module in `lib.rs` with one generic `wire_dep_into_linker` that walks `Component::component_type().exports(engine)` and registers proxy closures via `LinkerInstance::func_new(name, \|_store, _ty, params, results\| func.call(store, params, results))`. ~80 LoC of per-dep glue deleted, ~90 LoC of generic code added — net neutral but N-extensible instead of N-hardcoded. Trade-off: per-call `Val` boxing instead of compile-time typed wrappers (negligible for ~1 Hz calls like markdown render at composition; revisit if a 60 Hz dep appears). Logs `loader: dep <name> instantiated; wired N fn(s) across M interface(s) into consumer linker (generic)` for both markdown + emoji. Future system components install + run with ZERO wandr-host changes. See `tasks/39-generic-dep-wiring.md`. |
| 40 | Emoji-picker — second concrete cross-app dep system component | ✅ device-verified 2026-05-26. New `emoji-picker/` Rust cdylib (~70 emojis × 8 categories curated, static table in the binary). `wit/emoji.wit` (`wandr:emoji/picker.list-all() -> list<emoji>`). Hand-written `wandr-app/src/wasmWasiMain/kotlin/EmojiImports.kt` — 24-byte record lift (three strings × 8 bytes, align 4). `EmojiCard.kt` Compose card with category-grouped FlowRow grid. Purpose: proves cross-app deps are repeatable (not markdown-specific) and forced the generic dep wiring refactor (task 39) — both shipped together. Logcat: `emoji-card: list-all() → 72 emojis`. See `tasks/40-emoji-picker.md`. |
| 41 | System-fonts loader — third cross-app dep + serif typography in MarkdownCard | ✅ device-verified 2026-05-26. New `system-fonts/` Rust cdylib reads `/system/fonts/` via WASI preopen (wandr-host preopens it at `/system-fonts` in both standalone + run_once). `wit/system-fonts.wit` (`wandr:fonts/loader.{list-all, load}`). Compose's `FontFamily.Serif` / `FontFamily.Monospace` now actually render in NotoSerif / DroidSansMono — required three coordinated changes: (a) host `canvas_impl.rs::family_alias_paths` maps Compose's Linux-platform family aliases ("Noto Serif" / "Noto Sans Mono" / etc.) to actual /system/fonts/Noto*.ttf paths for the text-blob path; (b) host `paragraph_impl.rs::push_text_style` does the same lookup for the paragraph path (paragraph rendering doesn't call get_typeface — sets typeface directly on Skia TextStyle); (c) **SKIKO fix** in `org/jetbrains/skia/paragraph/TextStyle.kt` — `fontFamilies` array setter now mirrors first family into the singular `fontFamily` string. Without (c), Compose's `res.fontFamilies = resolved.aliases.toTypedArray()` write didn't reach the WIT layer (singular `fontFamily` stayed `""`), defeating any custom-family attempt. wandr-app gains `FontsImports.kt` + `FontsCard.kt` (208 files / 175 families on Pixel 2 XL). MarkdownCard's H1/H2/body all render NotoSerif; code-block + inline code stay DroidSansMono. Logcat: `get_typeface: loaded /system/fonts/NotoSerif-Regular.ttf` + `loaded /system/fonts/DroidSansMono.ttf`. See `tasks/41-system-fonts.md`. |
| theme | System theme (dark/light) + user-settable accent color | ✅ device-verified 2026-05-26. New `interface theme` in `wit/skiko-gfx.wit` exposes `get-night-mode -> enum {auto, off, on}` (host shells out to `cmd uimode night`) + `get-accent-color -> u32 ARGB` (host reads sysprop `persist.sys.wandr.accent`, returns 0 if unset). wandr-app's `RealComposeApp` reads both at composition: night-mode picks `darkColorScheme()` / `lightColorScheme()`; non-zero accent overrides `primary` + `primaryContainer` + `onPrimary` (Rec. 709 luminance picks onPrimary black/white). Live-update / watcher deferred — restart wandr-host between flips. **Material You proper not available** on Pixel 2 XL / LineageOS stack (`dumpsys wallpaper` shows `isColorExtracted=false`); user-settable sysprop is the practical substitute with the same WIT shape — swap to rsbinder/IWallpaperManager when a device that actually has Material You appears. Device-verified: dark↔light + green/orange/default accent screenshots. See `tasks/system-theme.md`. |
| 42 | System clipboard via rsbinder to IClipboard | 🔲 scoped 2026-05-26, not started. In-app clipboard already works; this task is for cross-app sync with browser/messenger/etc. Needs AIDL submodule extension for `android.content.IClipboard` + `ClipData` + `ClipDescription` + the recursive `AttributionSourceState` (hits the known `rsbinder-aidl-recursive-limitation` — hand-encode workaround). SELinux may also need `setenforce 0` for testing. Estimated 4-6 hours (much heavier than theme's 1.5h). Deferred until a concrete user trigger ("I want to paste from Chrome"). See `tasks/42-system-clipboard.md`. |
| 57 | System launcher / home | ✅ device-verified 2026-05-29 (Proposal A). Steps 1–6 + the dedicated `wandr.launcher` wandrpkg all shipped. **Dedicated launcher = a LIGHT Rust canvas guest** (`apps/system/wandr.launcher/`, ~70 KB `wasm32-wasip2`): exports `my:skiko-gfx/renderer`, draws the app grid directly via the canvas WIT — no Kotlin/Compose → leak-immune + tiny working set (right for a persistent home process). egui rejected (mesh/GL model doesn't fit our high-level host-Skia canvas WIT). Solved: guest wit-bindgen 0.46 rejects `skiko-gfx.wit` (`matrix-3x3`) → hand-authored trimmed `my:skiko-gfx` WIT with `paint-attrs`+enums verbatim; component imports a subset of the host's canvas, links cleanly (first non-Kotlin renderer guest). Layout built once → no per-frame churn. Verified: grid renders (title+letter-tiles+labels via host fonts), tap chains Rust hit-test → `launch-app` → arbiter launch+foreground. Wired into `build-system-wandrpkgs.sh` + set as default boot home (`WANDR_HOME_APP=wandr.launcher`). **Step 6**: `run-hybrid-stack.sh` + Magisk `service.sh` now `set-home` the launcher at startup (`WANDR_HOME_APP`, default `com.example.wandr-app`) → device boots straight to the launcher with no adb (verified fresh-state, no manual launch). **Step 1** (`7d6575f3`): arbiter home-app concept — `home_app_id` + persistence, `set-home`/`go-home`, boot-foreground, and fall-back-to-home in `handle_child_exit` (task 54 hook). **Steps 2–4**: `my:skiko-gfx/launcher` WIT (`list-apps -> string`, `launch-app`); `launcher_impl.rs` scans `<APPS_ROOT>/apps/*` for id+`label` (flat manifest), forwards `launch-app` to the arbiter socket. **Step 5**: `LauncherImports.kt` (canonical-ABI bindings, modeled on AssetsImports) + `LauncherCard.kt` (FlowRow letter-tile grid + theme gradient + tap→launch) in wandr-app; `label="Demo"`. Verified: set-home boots to the grid; tile renders "D"/"Demo"; tap chains guest→host→arbiter launch+foreground. See `tasks/57-launcher.md`. |
| 59 | dioxus canvas-WIT renderer ("tiny Blitz") + a real dioxus guest | ✅ device-verified 2026-05-29. A *light reactive* Rust UI framework over the canvas WIT — the Compose alternative for richer guests (settings, shade, file picker) without Compose's 15.7 MB / continuation-leak / ~180 MB working set. New `crates/dioxus-canvas/` (first entry in a new top-level `crates/` bucket for shared guest-side libs; on **dioxus 0.7** since task 60): `VirtualDom` → retained node arena → `taffy 0.7` flexbox → canvas-WIT draw verbs → pointer hit-test → dioxus event → re-render. **Renderer is WIT-agnostic via a `CanvasSink` trait** (the guest forwards it to its own trimmed-WIT `canvas::*`; keeps the lib host-unit-testable — `tests/render.rs` validates render + click→count 0→1). **Key learnings**: (a) `rebuild_to_vec()` drops the `Template` payload — a real renderer MUST implement `WriteMutations` (stack machine over compile-time templates, like dioxus-web/tui); `replace_placeholder` paths are relative to the template root, so pop the `m` nodes first. (b) dioxus-html needs a global `HtmlEventConverter` installed (`set_event_converter`) or the first event panics; the `Serialized*` data types are `serialize`-feature-gated (kept off) so a `HasMouseData` is hand-rolled. **Zero new host WIT** — taffy text-leaf measurement reuses the existing `paragraph` interface (task 14): build single-run paragraph → `layout` unconstrained → `get-max-intrinsic-width`+`get-height` → drop (cached in-guest). (An initial `measure-text` verb was added then removed in favour of this.) Demo `apps/user/wandr.dioxus.demo/` (**~520 KB**, < 600 KB target; trimmed WIT like wandr.launcher; counter + button + 4-item list). Device: flexbox column renders with host fonts (no clipping), tap→signal→repaint (count incremented), stable 68 s no leak/SIGILL. Wired into `build-system-wandrpkgs.sh`. Follow-ups (deferred per scope): input debounce (count advanced > tap count), scrolling/overflow, popups, keyboard events. See `tasks/59-dioxus-canvas-renderer.md`, `repos/dioxus-spike/`. |
| 60 | Unblock dioxus ≥0.7 for wasm32-wasip2 (subsecond/wasm-bindgen wall) | ✅ adopted (option B) + device-verified on dioxus 0.7.9, 2026-05-29; upstream PR (C) prepared. Vendored `external/subsecond/` (patched 0.7.9 + README) + `[patch.crates-io]` in dioxus-canvas + wandr.dioxus.demo, both bumped to 0.7; `events.rs` got the 0.7 tweaks. Demo builds for wasip2 with ZERO wasm-bindgen + renders on device (host fonts OK). Root-caused 2026-05-29: Task 59 pinned dioxus **0.6** because 0.7 won't build for our guest target. Cause (NOT "component model unsupported"): `dioxus-core 0.7` non-optionally deps `subsecond` (hot-patch engine; no feature disables it), which declares `wasm-bindgen`/`js-sys`/`web-sys` under `cfg(target_arch = "wasm32")` — matching wasip2 too, though its only use (`apply_patch`→`web_sys::window().fetch()`) is browser-only dev hot-reload. wasm-bindgen's `__wbindgen_*` intrinsics don't link under `wasm-component-ld`. `main` is NOT fixed. **Clean fix (proven — links + valid 216 KB component, zero wasm-bindgen in tree):** narrow subsecond's wasm `cfg` to `all(target_arch = "wasm32", target_os = "unknown")` (browser=unknown, wasip2=wasi) — 4 dep lines in subsecond `Cargo.toml` + 1 `cfg` at `lib.rs:550`; `apply_patch` then no-ops to `Ok(())` on wasip2 (we never hot-reload guests). dioxus-canvas core is otherwise 0.7-compatible (only `events.rs` needs `convert_cancel_data` + a `Modifiers` import tweak). Adoption: vendor/fork the patched subsecond + `[patch.crates-io]` per guest, and/or upstream the cfg-narrowing PR. See `tasks/60-dioxus-0.7-wasip2-subsecond.md`, [[reference_dioxus_07_wasip2_subsecond]]. |
| 61 | dioxus-canvas component showcase + renderer capability expansion | ✅ all 3 phases device-verified 2026-05-29. `wandr.dioxus.demo` → a 5-tab Compose-style gallery (Inputs / Pickers / Calendar / Color / Text): checkbox, switch, radio, stepper, progress, dropdown (inline-expand), color swatches, calendar, **slider (drag)**, **HSV color picker (drag)**, **text edit box + real soft keyboard (IME)**. Grew the `dioxus-canvas` renderer in 3 phases (each device-verified + host-tested): **P1** click-only components + tab nav (one renderer tweak: `border-radius:50%` → circles/pills via rrect clamping). **P2** drag — `on_pointer_{down,move,up}` dispatch dioxus `mousedown`/`mousemove`/`mouseup` with **pointer capture** + **element-relative coords** (`element_coordinates()`); HSV gradients discretized into solid cells (no gradient primitive); device-verified by an `input swipe` moving the hue blue→magenta. **P3** keyboard — `convert_keyboard_data` maps host `(code-point, key-id)` → dioxus `Key`, `F_KEY` focus tracking, `on_key` → `keydown` to the focused field; the field calls the existing host `ime` interface (`notify-editor-attached`) so `wandr.ime.keyboard` shows, and its keys route back via `ime-send-key-event`→socket→`on_key_event_v2` (device-verified: tapping the soft keyboard typed into the dioxus field). **No new host WIT verbs** — reuses `paragraph` (text measure), `ime`, `on-key-event-v2`. 5 host unit tests (render/click, conditional-render, drag, keyboard). ~624 KB. See `tasks/61-dioxus-component-showcase.md`. |
| 55 | System status bar (top strip) | 🟢 device-verified 2026-05-29 (Proposal A — top-overlay Rust canvas guest). Shows `wandr · clock · battery` on an 88px top strip over the launcher. **Surface abstraction reworked (design discussion):** the libgui overlay shim is now **geometry-parameterized** — `sf_create_overlay_surface(x,y,w,h)` (`w/h<=0`→full, `y<0`→bottom-anchored); status bar passes `(0,0,0,88)`, IME `(0,-1,0,1200)`. No per-anchor shim functions → new bars need no a-03 rebuild. Runtime owns semantics (`OverlayMode{None,Bottom,Top}`); arbiter is the per-process surface-role manager. ART-free data: `my:skiko-gfx/status` (`clock-text` via native `date`, `battery-text` via sysfs) + `status_impl.rs`. Guest `apps/system/wandr.statusbar/` (~48KB wasm32-wasip2). Rebuilt `libsf_surface.so` on a-03 (direct-ninja). v1 limits: no content insets (floats over top ~88px), `wandr.statusbar` under `apps/` (launcher lists it — move to `system-apps/`). Follow-ups: insets, arbiter `launch-overlay-top` + boot, notification area. See `tasks/55-status-bar.md`. |
| 62 | Orientation for overlay surfaces (IME + status bar + taskbar) | ✅ device-verified 2026-05-30. Overlays now rotate with the device — folds in sibling **task 58** (chrome rotation). Generic per-package `orientation = "auto"\|"locked"` manifest flag (default locked; NOT in the AOT cache-key), parsed in `app_installer.rs`, read at runtime via `app_loader.rs::LoadedApp::rotation_policy()`; `standalone.rs` gate `rotates = rotation_policy() \|\| mode==None` (fullscreen always rotates, overlays opt in — `wandr.ime.keyboard`/`wandr.statusbar`/`wandr.taskbar` all flagged). a-03 shim grew `sf_set_overlay_geometry(x,y,w,h)` (superset of `sf_resize_overlay`) + `sf_panel_dims()`. Unified anchor-aware `overlay_rect`: each strip placed at its USER-space edge (handedness 0→S/3→N/4→W/7→E; status bar=user-top, taskbar=user-bottom, IME=user-bottom offset `tb`), landscape IME depth scaled `t·pw/ph`. **IME guest (Compose) needed the same task-43 render-delegate `w/h` resize fix wandr-app had** (it discarded per-frame dims → landscape overflow). `canvas_impl.rs::resize()` now `recompute_transform`s. Redeploy lesson: `pkill -x wandr-host` (not `-f`) + kill the zygote — repeated redeploys stacked stale overlays. Follow-up done in **task 63**. See `tasks/62-overlay-orientation.md` + [[project-overlay-orientation]]. |
| 63 | App-driven orientation lock propagates to the chrome | 🟢 deployed 2026-05-30 (host-verified; physical-rotation confirm pending). An app's `orientation = "locked"` keeps it portrait AND locks the system chrome portrait while it's foreground. Gate fixed so a *fullscreen* app respects explicit `locked` (`mode==None ? !orientation_locked() : rotation_policy()`; absent still rotates — no task-43 regression; `app_loader.rs::orientation_locked()`). Cross-process signal = a global lock file `/data/local/tmp/wandr-orient-lock` (`1`=portrait) published by the **foreground fullscreen app** on its `AppRole::Foreground` transition (the status bar/taskbar are launched directly, not arbiter-tracked, so a per-socket push can't reach them); overlays read it each frame and force portrait while set (track `last_dev_orient` to re-apply on lock toggle). Standing config: **launcher = locked** (home stays portrait + locks chrome), **apps = auto**. `standalone.rs` (gate + `publish_orientation_lock`/`orientation_lock_active` + lock-aware poll), `build-system-wandrpkgs.sh` manifests. See `tasks/63-app-orientation-lock.md`. |
| 64 | On-demand rendering (stop the 60 fps idle CPU burn) | ✅ ALL STEPS device-verified 2026-05-30 — every guest (Rust canvas launcher/statusbar/taskbar + dioxus + Compose wandr-app/IME) now frame-paces; idle CPU ~7–11% (was launcher 23/IME 30/wandr-app 45%), full chrome stack ~140%→~30%. Plus: per-app `max_fps` cap (`WANDR_MAX_FPS` env > package.toml > 60, host-side), the dioxus `launch!` macro (a guest is pure dioxus + one line, deps only on dioxus+dioxus-canvas; wit-bindgen 0.46→0.57.1), and the cap-latency fix (don't gate input renders). **Root cause of high CPU** (measured): every guest renders 60 fps *unconditionally* (game-loop, not on-demand) — `standalone.rs` calls `render_frame`+`eglSwapBuffers` every 16 ms regardless of change. Static launcher (23%) / taskbar (15%) / hidden IME (30%) / status bar (20%) + idle dioxus (55%, **not** animating) = ~140% user + 57% sys. Fix = the Android Choreographer / Compose `hasInvalidations()` model: a new OPTIONAL `frame-pacing` WIT interface (`next-frame-delay() -> u32` = ms until next wanted frame; 0=animating, N=timed wake, large=idle), host gates the expensive render but cheap-polls input for latency. Incremental (optional export → no breaking change): **host+WIT scaffold → Rust canvas guests (launcher/bars) → dioxus → Compose/skiko LAST** (heavy republish). Start with host + Rust guests. See `tasks/64-on-demand-rendering.md` + [[reference-on-demand-rendering]]. |
| 56 | System taskbar / navigation (bottom strip) | 🔲 proposal 2026-05-29. The wandr launcher + app-switcher + nav, since the Android launcher is force-stopped and the user has no on-device way to switch/launch/go-home. A *view+controller over the arbiter* (which already owns the running-app registry + launch/foreground/kill). Proposals: A = guest wandrpkg `wandr.taskbar` on a bottom overlay driving a new `wandr:shell/launcher` WIT forwarded to the arbiter (recommended); B = host-drawn MVP; C = edge gestures (home/back/recents via InputFlinger drain — complement, not replacement). Needs `package.toml` `label`+`icon` manifest fields (installed-app enumeration) + IME-coexistence policy (taskbar hides while keyboard overlay shows). See `tasks/56-taskbar.md`. |
| 43 | Real screen orientation handling in standalone mode | ✅ device-verified 2026-05-29. Auto-follows the device (both landscape directions, full-screen, taps correct). **The "needs a-03" framing was wrong** — no SF buffer-transform shim, no EGL resize: the host-side `WANDR_ORIENT` machinery already does content pre-rotation via Skia `base_matrix` (8-orientation dihedral group); runtime rotation just makes it dynamic. Rotation source is the native **Device Orientation HAL sensor** (`android.sensor.device_orientation` type 27, on-change) read via the rsbinder sensorservice path — the same sensor WMS reads, but ART-free (no `system_server`). Landed: `canvas_impl.rs` (`dihedral_transform()` + `set_orientation()` + `current_orient`), `sensors_impl.rs` (`find_handle_by_type` + `device_orientation_handle`/`enable_sensor`/`poll_device_rotation`), `standalone.rs` (enable+poll+apply, pointer inverse-transform, `device_rotation_to_orient`, gated to fullscreen app, `WANDR_ORIENT` override). Plus a wandr-app fix (`Main.kt`/`RealComposeApp.kt`): the render delegate was discarding the per-frame `w/h`, so the scene stayed portrait-sized and `base_matrix` rotated portrait content into the landscape buffer (half-render) — now applies `realScene.size` + popup `containerSize` on change (pre-existing latent bug, no skiko rebuild). Orientation lock deferred to a future `package.toml` manifest field (not a WIT verb). See `tasks/43-screen-orientation.md`. |
| 44 | WMS window registration for non-Activity processes (path A from task 40 session 6) | 🟡 session 7 done 2026-05-27, **POSTPONED 2026-05-27** — architecturally at odds with locked §9 Hybrid-zygote runtime model. Session 7 vendored all 4 WMS AIDLs via the build.rs slot-stub pattern (154+42+16+1 methods), new `wandr-host/src/wms_impl.rs` modeled on `ime_impl::session3`, `--probe-wms-opensession` CLI flag. **Outcome**: rsbinder transport ✅ to `system_server`'s `window` service; WMS threw `IllegalStateException: Unknown pid=N uid=0`. Gate located precisely via AOSP source: `Session.java` constructor does `mAtmService.mProcessMap.getProcess(pid)` and throws on null. Forward path (vendor `IActivityManager`, call `attachApplication`) is a dead end — `attachApplicationLocked` calls `killProcessQuiet(pid)` when the pid is not in `mPidsSelfLocked`/`mPendingStarts` (i.e. not Zygote-via-AMS spawned). **Why postponed**: in the locked Hybrid model (see [[project-app-lifecycle-and-packaging]]) our own arbiter spawns apps — no AMS / ATMS / WMS for app windows post-ART; task 44's goal of "Gboard from standalone-via-su" is transitional and becomes moot. Resume only if a concrete short-term need arises. See `tasks/44-wms-window-registration.md` "Postponement" section. |
| 40-ime | Real IME via rsbinder → IMMS — multi-session arc | 🟡 session 5 done 2026-05-27 — showSoftInput lands; WMS focus gate identified. **Session 1** (`fbddb528`): scope doc. **Session 2** (`4bbb218a`): vendor frameworks-base + `isImeTraceEnabled()`. **Session 3** (`a8d266f0`): addClient + Bn-side callback servers; non-Activity registration accepted. **Session 4** (`6551a6b1`): startInputOrWindowGainedFocus + fabricated windowToken; WindowToken validation against WMS does NOT block; rsbinder UnexpectedNull-as-success quirk for null parcelable returns. **Session 5** (2026-05-27): renamed `ImeTracker.aidl` stub to flat name (dropping `parcelable ImeTracker.Token;` Java nested-class syntax that rsbinder-aidl 0.7.0 emits as invalid Rust); un-stubbed `slot_08_showSoftInput` with full 8-arg signature; new `probe_showsoftinput()` runs the three-step `addClient → startInput → showSoftInput` sequence end-to-end with `SHOW_FORCED` flag. **Outcome**: binder pipeline ✅ (transport, parcels, identity, all three calls land at IMMS cleanly), Gboard ❌ (IMMS gates show on WMS-tracked focused window; the launcher is focused per WMS, not us). Logcat: `W InputMethodManagerService: Ignoring showSoftInput of uid 0 ...` + `ImeTracker: onFailed at PHASE_SERVER_CLIENT_FOCUSED`. `dumpsys input_method` correlation: `mFocusedWindowClient=ClientState{mUid=10167 ...}` (the launcher), `mImeWindowVis=0`, `mInputShown=false`. The IME-summon gate is exclusively WMS focus state — `SHOW_FORCED` doesn't bypass it. **Session 6** (2026-05-27, scoping-only — no code): confirmed empirically that even task-33 standalone surface (which DOES register an InputDispatcher input window via `IInputFlinger.createInputChannel` + `gui::WindowInfoHandle` + `setInputWindowInfo`, visible in `dumpsys input` as `Window: wandr - wandr`) does NOT propagate to IMMS — `mFocusedWindowClient` stays pinned to the launcher (uid=10167) even with InputDispatcher focus on 'wandr'. IMMS uses WMS focus tracking, not InputDispatcher's; the two are separate subsystems and `setInputWindowInfo` bypasses WMS entirely. `IWindowManager.aidl` has 146 methods + ~30 transitive AIDL imports (Bitmap, Configuration, KeyboardShortcutGroup, …); `IWindowSession.addToDisplay` alone needs IWindow + WindowManager.LayoutParams (~30 fields) + InputChannel + InsetsState + InsetsSourceControl + MergedConfiguration + ClientWindowFrames + InputTransferToken. WMS vendoring is a multi-week project of substantially different shape than sessions 1-5. **Status: 🟢 binder protocol complete; paused at WMS gate.** Recommended path: park task 40, revisit when standalone mode becomes primary and the in-canvas Compose keyboard's English-only / no-voice / no-emoji-picker limitations become real user pain. The in-canvas keyboard (per `feedback_softkeyboard`) works for current needs. Three forward paths documented in the task doc: A=vendor IWindowManager (proper, multi-week, spin out as new task), B=NativeActivity wrapper (JNI-extract activity windowToken), C=park (recommended now). See `tasks/40-real-ime.md` "Session 5 results" + "Session 6 results". |
| 45 | wandr-zygote MVP spike — fork+COW native runtime model (Hybrid §9 architecture) | ✅ device-verified 2026-05-27. Native Rust zygote (`wandr-host --zygote`) preloads `wasmtime::Engine`, listens on UNIX socket `/data/local/tmp/wandr-zygote.sock`, `fork()`s on `LAUNCH <app-id>` (headless `wasi:cli/command`) or `LAUNCH_GUI <app-id>` (full Compose render loop). 5 steps, all complete: (1) fork + headless cwasm smoke (md-smoke-rust ×2 concurrent, ~25 ms per launch); (2) EGL re-init in child + SF surface — **Adreno EGL survives fork() cleanly** (keystone unknown resolved); (3) COW measurement vs no-zygote baseline — 5.6 MB Shared_Dirty per child vs 0 baseline, attributed mostly to binary `.data/.bss` (4.7 MB) not Engine heap (200 kB); (4) two distinct apps concurrent — `com.example.wandr-app` installed wandrpkg alongside dev cwasm, both 60 fps independently; (5) close-out doc + task-46 recommendation. Decisions D1-D7 all held empirically: no fork-time landmines fired. Per-app working set ~180 MB dominates the zygote's COW savings (~5 MB/child) — Hybrid wins are about three-tier isolation, not memory. Spinning out production work: SIGCHLD reap, KILL command, Component preload registry (path to ≥20 MB COW per child), wandr-arbiter as a separate process, init.rc/sepolicy. See `tasks/45-wandr-zygote-spike.md` "What we learned" + "Recommended task 46 scope". |
| 46 | wandr-arbiter MVP — Hybrid runtime production prep | ✅ fully shipped 2026-05-27 (5/5 steps + crash-marker). New sibling `wandr-arbiter` cargo crate (~800 KB; codeberg.org/harryzz/wart-arbiter); policy daemon talking to the zygote socket. Step 1: SIGCHLD reaper thread + `KILL` socket cmd. Step 2: Component preload registry (auto-preload `<APPS_ROOT>/system-apps/*` at startup + on-demand `PRELOAD <app-id>` socket cmd; closes COW gap to 57.6 MB Shared_Dirty per render child — 10× engine-only baseline). Step 3: `wandr-arbiter` skeleton + 5 client cmds (launch / launch-headless / list / kill / preload). Step 4: arbiter fg/bg policy via SIGUSR1/SIGUSR2 + `app_role` atomic + sf_set_layer/set_visible (rebuilt libsf_surface.so on a-03 via 43 s direct-ninja path) + `/proc/<pid>/oom_score_adj` writes (fg=0, bg=500); `wandr-arbiter foreground <app-id>` swaps cleanly; LAUNCH auto-promotes. Step 5 (init.rc/sepolicy replacement): Magisk module `wandr-stack-magisk/` auto-starts both daemons at `late_start_service`; reversible via Magisk disable/remove flags; no destructive `/system` changes. Crash-marker: arbiter persists running-apps map + foreground to `/data/local/tmp/wandr-arbiter-state.json` after every command; restart re-attaches surviving children via `kill(pid, 0)` liveness probe (dead pids dropped + logged). One race bug found + fixed mid-smoke (SIGUSR2 default Term kills child before its handler installs; resolved by installing handlers in the zygote parent so children inherit via sigaction table). See `tasks/46-wandr-arbiter-mvp.md`. |
| 47 | IME via dedicated guest app (Path B) | 🟡 steps 1–3 + 3c shipped + IME-routing socket in place (2026-05-28). `wandr.ime.keyboard` wandrpkg renders a full keyboard on its own SurfaceFlinger overlay surface; key events round-trip arbiter → focused-app via per-host UNIX sockets. Steps 1–3c lit up by tasks 47-step-* commits; the IME's input-window bug (sf_surface display-coord vs layer-local touchableRegion + double-subtracted overlay-Y in `sf_input_poll`) resolved 2026-05-28 in wandr-host 0de1da2. **Step 4 (InputFlinger arbitration + auto-hide) and step 5 (polish) remain.** See `tasks/47-ime-via-guest-app.md`. |
| 48 | Runtime panel dim query via SurfaceComposerClient::getActiveDisplayMode | ✅ device-verified. Replaces compile-time panel-dim constants. See `tasks/48-panel-dim-query.md`. |
| 49 | IME content control — editor-driven layouts + language plugins | ✅ device-verified 2026-05-28. Six steps. Step 1 (`wandr:ime/ime` host→guest export + ime_inbound `EditorAttached` / `EditorDetached`). Step 2 (editor-type override drives Numeric/Phone/Email/Url/Password layouts; English QWERTY ported in from old in-canvas keyboard; cabi_realloc record-with-strings export bug forced primitive params, see `feedback_wasi_cabi_realloc_export_block`). Step 3 (`wit/keyboard-lang.wit` plugin contract; first plugin `wandr.lang.bg` as Rust cdylib exports `wandr:keyboard-lang-bg/lang@0.1.0`). Step 4 (second plugin `wandr.lang.fr`, AZERTY). Step 5 (per-plugin WIT package names — two deps cannot share a `linker.instance(name)` entry — so each lang has its own package; IME hardcodes the known plugin set in `LangAdapter.kt`; new `wrapLanguageLayout` injects digit/shift/utility rows around plugin letter rows; `loadAllLayouts` merges builtins + plugins). Step 6 close-out + memory `feedback_ime_layout_arbitration`. **Device-verified**: 🌐 cycles English → Български → Français → English cleanly. wandr-app's in-canvas keyboard removed. New `scripts/pack-ime-keyboard.sh` packs the IME wandrpkg with `[dependencies]`. See `tasks/49-ime-content-control.md`. |
| 50 | Multi-line cursor render — skiko-wasi paragraph lineMetrics | ✅ device-verified. See `tasks/50-cursor-render-multiline.md`. |
| 51 | Dynamic lang-plugin loading (host-mediated) | 🔲 scoped 2026-05-28, not started. Polish follow-up to task 49 step 5 — inverts the IME-knows-plugins model: host scans `system-apps/wandr.lang.*` at startup, exposes `enumerate-lang-plugins` + `get-lang-layout(lang-id, shifted)` via new WIT verbs, IME asks. Removes the IME's hard-coded `LangAdapter.plugins` registry + the per-plugin `@WasmImport` blocks + the `[dependencies]` block in `package.toml`. New languages drop in as `.wandrpkg` zero-touch. ~4-6h, host-heavy. Do when (a) a 3rd lang is needed, (b) we want to retire the hand-rolled canonical-ABI in `LangAdapter.kt`, or (c) external plugin distribution becomes a concern. See `tasks/51-dynamic-lang-plugins.md`. |
| 52 | Monorepo reorganization (directory layout + naming) | ✅ scoped + executed via task 53. `apps/{system,user}/`, `runtime/`, `wit/`, `external/`, `tools/`, `repros/`. `wandr-*` for native, `wandr.*` for wandrpkgs. See `tasks/52-monorepo-reorg.md`, `docs/repository-layout.md`. |
| 53 | Monorepo merge (subtree-import 13 sibling repos + 4 submodules) | ✅ executed 2026-05-28 single day. All first-party repos folded into the wandr monorepo with full history preserved; external forks wired as submodules under `external/`. 11/11 build checks green; IME 🌐 cycle device-verified post-reorg. 13 obsolete sibling codeberg repos archived. See `tasks/53-monorepo-merge.md` "Results" section. |
| 54 | Arbiter death notification + socket cleanup | ✅ device-verified 2026-05-29. Closes the steady-state lifecycle gap exposed by an LMK on 2026-05-29: wandr-app killed at `oom_score_adj=0` (foreground); arbiter never noticed because it isn't the parent of zygote-forked apps (SIGCHLD only sees own direct children); orphaned control socket left IME visually present but useless for character keys. Shipped: (A) zygote → arbiter death-notification — `SUBSCRIBE_EXITS` long-lived connection (`zygote.rs` `exit_subscribers()` + `broadcast_exit()` from the SIGCHLD reaper); arbiter subscriber thread feeds `handle_child_exit(pid, detail)` which reuses `demote_from_overlay()` (overlay teardown) + `state::remove()` + `remove_host_socket()`, all under a coarse `arbiter_lock()`. (B) 5 s `kill(pid,0)` polling-backstop thread (covers dropped subscriber / crashed zygote). Socket cleanup: death-driven unlink + `sweep_stale_host_sockets()` at zygote startup (cleaned 59 stale files on first run) + graceful-exit unlink in `standalone.rs`. Device repro (`kill -9` the fg app while an IME overlay is engaged): full cascade fired instantly on the subscriber thread (reaper → broadcast EXITED → on_child_exit → overlay-clearing → socket removed); post-kill `list` dropped the app and the IME lost `[fg]` (ghost-keyboard scenario resolved). See `tasks/54-arbiter-death-notification.md`. |

**What's verified on device:** BasicTextField + TextFieldState + hardware
keyboard, in-canvas soft keyboard, Material3 widgets (Button, Checkbox,
DropdownMenu, Switch, Slider), LazyColumn + scrolling, warm-resume across
lifecycle events, host-side WasiDrawable transforms, **vibrator HAL via
rsbinder binder transactions** (task 16), **AAudio playback (440 Hz
sine, stereo PCM-f32, 48 kHz)** via rsbinder → `media.aaudio` → MMAP
HAL (task 21), **Compose `LocalHapticFeedback` → WIT haptics adapter**
so Material3 widgets buzz the device (task 18).

**Known issue (not blocking):** indeterminate `ProgressIndicator` leaks
~0.4 MB/s in wasm linear memory due to Kotlin/Wasm continuation retention
in `while(true){ withFrameNanos {} }` loops. Mitigation: use static
`progress = { 0.5f }` widgets. See feedback memory
`feedback_indeterminate_progress_leak`.

**Next work** (scoped, not started): tasks 19 (IPower+IThermal), 20
(sensors), 21 (audio playback) — see `tasks/` for details. Longer-term
direction in `post-art-roadmap.md`. Also outstanding: IME / soft-keyboard
production polish (JNI to Android `InputMethodManager`), Skottie / shaper
for Lottie animation, more exotic ColorFilter modes, address the
ProgressIndicator memory leak. Task 18 (Compose `DefaultHapticFeedback`
adapter → WIT haptics) is the natural follow-up to tasks 16/18 once
device-side HAL is wanted from Compose `performHapticFeedback()`.

---

## Task file index

WIT-surface expansion notes (tasks 8–14) describe how each Skia/Compose
feature was originally scoped — implementations landed organically during
the Compose port. HAL-pipeline tasks (15–17) are recent and were executed
sequentially; tasks 19–21 are scoped follow-ups for the runtime-to-HAL
boundary from `post-art-roadmap.md` §3.

| # | File | What it covers |
|---|------|----------------|
| 8 | `tasks/08-paint-completeness.md` | blendMode, strokeCap/Join, arc, drrect, clip-rrect, skew, matrix concat — ✅ |
| 9 | `tasks/09-path-pathbuilder.md` | Path + PathBuilder via SVG string, drawPath, clipPath — ✅ |
| 10 | `tasks/10-textblob-builder.md` | TextBlobBuilder multi-run text, drawTextLine — ✅ |
| 11 | `tasks/11-gradient-shaders.md` | linear + radial gradient shaders, Shader class — ✅ |
| 12 | `tasks/12-image-rect.md` | drawImageRect, Image Kotlin class — ✅ |
| 13 | `tasks/13-color-filter.md` | ColorFilter tint + invert — ✅ |
| 14 | `tasks/14-paragraph.md` | Paragraph layout + getRectsForRange / getGlyphPositionAtCoordinate / getWordBoundary — ✅ |
| 15 | `tasks/15-rsbinder-pipeline.md` | rsbinder + rsbinder-aidl pipeline; AOSP HAL AIDL submodule; SDK 29→30 bump — ✅ |
| 16 | `tasks/16-vibrator-hal.md` | Vibrator HAL via IVibrator binder; @nullable workaround via manual parcel — ✅ device-verified |
| 17 | `tasks/17-lights-hal.md` | New WIT lights interface; ILights binder; graceful no-op on devices w/o AIDL HAL — ✅ |
| 19 | `tasks/19-power-thermal-hal.md` | IPower performance hints + IThermal read-only state — ✅ device-verified; submodule bumped to android-15.0.0_r36 |
| 20 | `tasks/20-sensors-hal.md` | ISensorManager (frameworks-layer AIDL); pull-model sensor sample polling; first Bn-callback server — ✅ device-verified |
| 21 | `tasks/21-audioflinger-playback.md` | Audio playback via rsbinder→IAAudioService; primitives factored for reuse (binder_shared_memory + eventfd_signal) — ✅ device-verified |
| 18 | `tasks/18-compose-haptic-adapter.md` | Compose `LocalHapticFeedback` provider → WIT haptics; closes the Compose-UI ↔ vendor-vibrator-HAL loop set up in task 16 — ✅ device-verified |
| 22 | `tasks/22-isurfacecomposer-roundtrip.md` | rsbinder probe of `SurfaceFlingerAIDL` (`android.gui.ISurfaceComposer`); roadmap §5 de-risk for the eventual boot-model migration — ✅ device-verified |
| 23 | `tasks/23-profiling-hooks.md` (+ scope `tasks/scope-profiling-tools.md`) | Wire `ResourceLimiter` + `GuestProfiler` + per-frame data_size + call_hook behind a `profile` cargo feature; characterizes the ProgressIndicator leak quantitatively + breaks down the ~10–20 ms/frame budget — 🟡 MVP done |
| 24 | `tasks/24-bisect-wasm-leak.md` (+ reproducer `wandr-leak-repro/`) | Bisect the ~8 MB/min WasmGC-heap leak from task 23 down to its root. Step 1 done: bare `suspendCoroutine` loop with no Compose / no kotlinx-coroutines leaks at ~9 MB/s, OOMs Pixel 2 XL in 6:37. Isolated to Kotlin/Wasm `suspend` codegen — 🟡 step 1 done |
| 25 | `tasks/25-diagnose-suspend-leak.md` | Diagnostic deep-dive on task 24's leak: tighten reproducer (eliminate WasiScheduler), identify the leaked structref class via wasm-tools dump + patched wasmtime live-object summary, read kotlinc-wasm-backend codegen to find the missing slot-clear — 🔲 scoped |
| 26 | `tasks/26-store-worker-thread.md` | Move wasmtime Store to a worker thread to avoid ANR from long Store::gc cascades. Implemented end-to-end + device-tested; eliminated ANR but introduced worse input-lag accumulation (5-6 s after minutes). Reverted as net regression — ❌ attempted+reverted |
| 27 | `tasks/27-skiko-image-shader-gaps.md` | Implement the WIT-shaped skiko stubs: Image.makeFromEncoded, Shader.makeSweepGradient, Image.makeShader, Shader.makeBlend, Gradient-object overloads — ✅ device-verified 2026-05-18. Bitmap.makeShader deferred (no host-side Bitmap state) |
| 28 | `tasks/28-skiko-abstract-canvas.md` | Wire the abstract org.jetbrains.skia.Canvas's 41 throw-stubs to host-side skia via 38 new bc-* WIT verbs + per-Bitmap host raster Surface. Unblocked SegmentedButton (checkmark + unselected labels visible) and DatePicker render/swipe/year-pick. Chevron taps remain blocked by an orthogonal Material3 TooltipBox bug ([[tooltip-sigill-wasi]]). ✅ device-verified 2026-05-19 |
| 29 | `tasks/29-tooltip-sigill-bisect.md` | Diagnose the Material3 TooltipBox SIGILL on wasi. **Characterized end-to-end 2026-05-19, workaround deferred.** Trigger: clickable Press / long-press timeout → `BasicTooltipState.show()` → `suspendCancellableCoroutine`+`withTimeout` → kotlinx `Delay` → WASI adapter `poll_oneoff` → Rust `assert!` → SIGILL the wasmtime signal handler fails to convert to a Trap on Android. Mitigation in place: wandr-app side disables TooltipBox-wrapped widgets (DatePicker chevrons, etc.). Step 4 deferred — see task doc "Step 4 decision" for the four workaround paths considered + why none was taken now. 🟡 characterized |
| 30 | `tasks/30-wasi-adapter-assert-and-wasmtime-signal-handler.md` | Spin-out of task 29 Step 3 — investigate and patch the root cause. Two stacked bugs: (a) WASI P1 reactor adapter trips a Rust `assert!` inside `poll_oneoff` when driven by kotlinx-coroutines `Delay`/`withTimeout` on wasi; (b) wasmtime's signal handler on Android fails to intercept the registered `unreachable` trap, so the process aborts to debuggerd instead of returning Err. 6 steps: capture assert msg via wasi-stderr→logcat, rebuild adapter with DWARF, identify failing precondition, diagnose wasmtime sigaction-on-Android, verify against `TooltipInspectionCard` (kept in place for this purpose), audit related bugs ([[kotlin-wasm-suspendcoroutine-leak]] etc). ✅ fully closed 2026-05-20 (Step 4 deferred w/ re-open criteria; Step 6 audit found no extra retroactive fixes). |
| 33 | `tasks/33-boot-model-bringup.md` | Boot-model bring-up sub-roadmap — get the runtime running as a standalone privileged process (no `NativeActivity`), owning display + input directly, per `post-art-roadmap.md` §11. 5 ordered steps, keystone first: standalone-surface spike (non-Activity `su`-process → fullscreen `SurfaceControl` from SurfaceFlinger via libgui → EGL one frame). Self-contained for a fresh session. 🔲 scoped |
| 34 | `tasks/34-kt86415-fixed-partition.md` | KT-86415 real fix (Option B) — the WASI adapter's 64 KB `State`, `cabi_realloc`'d into Kotlin's `ScopedMemoryAllocator` bump region, was reused after `freeAll` → use-after-free. Fix: static linear-memory partition — `[0x10000,0x20000)` reserved for `State`, Kotlin's root allocator starts at `RESERVED_BASE=0x20000`; one constant in `MemoryAllocation.kt` + the adapter's `State::new`. Kills UAF + leak, no heuristic, `destroy()` stays stock. ✅ device-verified 2026-05-21 — see the status-table row above. |
| 35 | `tasks/35-app-install.md` | App installer (+ thin loader) — the multi-component package boundary. `wandr-host/src/app_loader.rs` (AppRef enum + LoadedApp + AppLoader trait + WandrLoader; self-healing `AppRef::Installed` reader) + `wandr-host/src/app_installer.rs` (WandrInstaller calls `Engine::precompile_component`, writes spec-shaped install dir + `cache-key.toml`). CLI: `wandr-host --install <wandrpkg>` + `--standalone --app <id>`. Cache layout parallels Android dex2oat `/data/dalvik-cache/` — apps ship `.wasm`, runtime AOTs `.cwasm` per-device + invalidates on engine/wasm drift. Single-component only — multi-component composition deferred to `tasks/36-cross-app-deps.md`. ✅ device-verified |
| 36 | `tasks/36-cross-app-deps.md` | Cross-app dependencies + system components — `[dependencies]` table walking (host-WIT / runtime-bundled / user-app), two composition modes (same-Store library-like vs separate-Store service-like with host proxy), install-time resolution against `/data/wandr/{apps,system-apps}/`, cache-key extension recording dep hashes (any dep update → A's hash flips → re-precompile). Wasmtime stable APIs (`Linker::instance`/`substituted_component_type`/`resource`/`instantiate`) cover everything; true lazy linking still unstable (separate-Store + `OnceCell` is the workaround for cold-path / heavy deps). ✅ device-verified end-to-end 2026-05-26 (steps 1–7 + visuals v2 — rich Compose document tree rendering). |
| 37 | `tasks/37-kotlin-wasm-command-adapter-throw.md` | Spin-out of task 36 step 7. Kotlin/Wasm 2.4 + the WASI COMMAND adapter throws "thrown Wasm exception" at module init unconditionally — empty `fun main() {}` throws identically; on-device confirmed (wandr-host's `wasi_stderr` doesn't help). Reactor adapter known-good. Blocks Kotlin CLI consumers; Rust CLI smoke (`md-smoke-rust/`) sidesteps. 5-step plan (capture exception payload via `Store::take_pending_exception` first, then bisect kotlinc/adapter versions, file upstream). 🔲 scoped, not started. |
| 38 | `tasks/38-wandrpkg-assets.md` | wandrpkg asset/data files. Bundle `assets/` auto-copies to `<install_dir>/assets/`; runtime exposes via new `my:skiko-gfx/assets.read` host WIT verb (path-safety guard rejects `..`/absolute/empty). Parallel WASI fs preopen at `/assets` also wired (free; ready for future Kotlin stdlib file APIs). Pivoted from WASI-fs-read-in-Kotlin to custom WIT because Kotlin/Wasm 2.4 stdlib has no `fd_read`/`path_open` wrappers. wandr-app's MarkdownCard reads `assets/demo.md` at composition. ✅ device-verified 2026-05-26. |
| 39 | `tasks/39-generic-dep-wiring.md` | Generic dep wiring via wasmtime introspection — replaces hardcoded per-dep `bindgen!` modules + match dispatch with one `wire_dep_into_linker` that walks `Component::component_type()` and registers proxy closures via `LinkerInstance::func_new`. Future system components install + run with zero wandr-host changes. Trade-off: per-call `Val` boxing vs typed wrappers (negligible for ~1 Hz dep calls). ✅ device-verified 2026-05-26 co-shipping with task 40. |
| 40 | `tasks/40-emoji-picker.md` | Second cross-app dep system component. `emoji-picker/` Rust cdylib (~70 emojis × 8 categories, static curated table). `wandr:emoji/picker.list-all() -> list<emoji>`. wandr-app's `EmojiCard` renders category-grouped FlowRow grid. Purpose: proves cross-app deps are repeatable (not markdown-specific); forced generic dep wiring (task 39) to land. ✅ device-verified 2026-05-26. |
| 41 | `tasks/41-system-fonts.md` | Third cross-app dep + serif typography. `system-fonts/` Rust cdylib enumerates `/system/fonts/` via WASI preopen at `/system-fonts`. `wandr:fonts/loader.{list-all, load}`. Plus three coordinated host/skiko changes that make Compose's `FontFamily.Serif` / `FontFamily.Monospace` actually render in NotoSerif / DroidSansMono: (a) `canvas_impl.rs::family_alias_paths` maps Compose's Linux-platform aliases to /system/fonts/* paths; (b) `paragraph_impl.rs::push_text_style` does the same lookup for the paragraph path and sets typeface directly on Skia TextStyle; (c) skiko's `TextStyle.fontFamilies` array setter mirrors first family into singular `fontFamily` (was empty before, defeating any custom family). wandr-app's MarkdownCard now renders headings + body in real NotoSerif. ✅ device-verified 2026-05-26. |
| theme | `tasks/system-theme.md` | System theme (dark/light) + user-settable accent. New `interface theme` in `wit/skiko-gfx.wit` with `get-night-mode` + `get-accent-color`. Host: `theme_impl.rs` shells out to `cmd uimode night` for the mode; reads `persist.sys.wandr.accent` sysprop for the accent ARGB. wandr-app's MaterialTheme branches dark/light + applies the accent (when non-zero) to primary/primaryContainer/onPrimary. Material You wallpaper-driven accent isn't available on Pixel 2 XL / LineageOS (`isColorExtracted=false`); sysprop is the substitute with the same WIT shape. ✅ device-verified 2026-05-26. |
| 42 | `tasks/42-system-clipboard.md` | System clipboard sync via rsbinder to `IClipboard`. Needs AIDL submodule extension for ClipData / ClipDescription / AttributionSourceState (recursive parcelable — hits `rsbinder-aidl-recursive-limitation`). In-app clipboard already works; this is for cross-app paste from browser/messenger/etc. Estimated 4-6h. 🔲 scoped, deferred until a concrete user trigger. |
| 43 | `tasks/43-screen-orientation.md` | Runtime screen orientation handling in standalone mode (currently fixed portrait — SurfaceFlinger doesn't auto-rotate non-Activity surfaces). 5-step plan: rotation poll → libgui Surface::setTransform shim → EGL/Skia resize → Compose on_resize → state preservation (warm-resume style). Blocker: shim step needs the AOSP a-03 build host. 3-5h focused work. 🔲 scoped, not started. |
| 44 | `tasks/44-wms-window-registration.md` | WMS window registration for non-Activity processes (path A from task 40 session 6). Spin-out of task 40's WMS focus gate. Session 7 vendored 4 WMS AIDLs via slot-stub pattern + `wms_impl.rs` + `--probe-wms-opensession` flag; openSession lands at WMS, rejected with `IllegalStateException: Unknown pid` (ATMS `mProcessMap` gate). Forward path (`IActivityManager.attachApplication`) is dead-end — `attachApplicationLocked` kills non-AMS-spawned pids. 🟡 **POSTPONED 2026-05-27** — architecturally at odds with locked §9 Hybrid model (see task 45); resume only if a concrete short-term Gboard need arises before Hybrid ships. |
| 45 | `tasks/45-wandr-zygote-spike.md` | wandr-zygote MVP spike (Hybrid §9 architecture). Native Rust zygote that preloads `wasmtime::Engine`, listens on UNIX socket, `fork()`s on `LAUNCH`/`LAUNCH_GUI <app-id>`. 5-step plan validates the technical Hybrid path: fork + headless cwasm (step 1), EGL re-init in child + render loop (step 2 — **Adreno EGL survives fork**, keystone unknown resolved), COW measurement vs baseline (step 3 — ~5.6 MB Shared_Dirty per child, mostly binary .data/.bss not engine heap), two distinct apps concurrent (step 4 — wandr-app + dev cwasm both at 60 fps), close-out + task-46 recommendation (step 5). D1-D7 design decisions all held empirically; no fork-time landmines. Per-app working set ~180 MB dominates zygote COW savings — Hybrid wins are about isolation, not memory. ✅ device-verified end-to-end. Production work (SIGCHLD reap, Component preload, wandr-arbiter, init.rc/sepolicy) spun out as recommended task 46. |
| 66 | `tasks/66` (see repros) | wasi:tls transport for Signal de-risk — host-side `TlsProvider` adds Signal's pinned CA to wandr-host so a wasm32-wasip2 guest reaches `chat.signal.org` over host-delegated `wasi:sockets`+`wasi:tls` with zero crypto in the guest. ✅ device-verified. See `[[reference_wandr_wasi_tls_transport]]`, `repros/wasi-tls-{probe,runner}`. |
| 67 | `tasks/67-signal-client.md` | Text Signal client as a wasm guest (zero crypto/networking compiled in; all transport over host wasi:tls). **Phase 0+1 ✅ desktop + on-device:** forked `libsignal-service-rs` transport onto wasi:tls (`external/libsignal-service-rs/wart-wasi-shims/`), link-as-secondary-device, receive+decrypt, **send**, pure-Rust WASI-fs persistence (resume w/o re-link + history). Working client: `repros/signal-link/`. **Phase 2 (next, scoped):** WIT-decoupled `signal-engine` (exports `wandr:signal/chat`) + `signal-ui` (dioxus/Compose over skiko-gfx), composed via WAC — engine needs a persistent step-executor (wstd block_on doesn't span component calls). 🟡 Phase 2 scoped, not started. See task doc "RESUME HERE" + `[[project_signal_wasip2_transport_swap]]`. |
| 75 | `tasks/75-ringrtc-signal-calls.md` | Signal 1:1 voice calls — graft Signal's call protocol onto the (completed) `crates/wandr-call` engine instead of porting ringrtc's C++ libwebrtc. ringrtc = Rust orchestration + C++ libwebrtc media; wandr-call already replaces the latter in pure Rust (libwebrtc-interop proven via `call-browser`). Head start: the `CallMessage` (Offer/Answer/IceUpdate/Hangup/Busy + `opaque`) signaling envelope is already vendored and rides the existing E2E Signal channel (task 67); mic↔AAudio + arbiter comms-session already built. Crux = decode/emit the `opaque` ConnectionParameters blob (ringrtc `signaling.proto`) ⇄ `wandr_call::Signaling`. Recommended approach B: reimplement the 1:1 protocol on wandr-call using ringrtc as the spec (AGPL caveat). 4 phases (opaque codec → two-wandr call → interop with a REAL Signal client → UI+arbiter). Scope v1: audio-only, 1:1-only (video=wasm-VP8 + group=SFU deferred). 🔲 scoped, not started — fresh session. |
| 75-impl | `tasks/75-ringrtc-signal-calls.md` (+ `75-NEXT-audio-output.md`) | **IMPLEMENTED + device-verified (2026-06-03, commit 6eb66444):** outbound Signal 1:1 call audio is **real voice** — full-frame Opus decode (peer sends 60 ms; decode at exact TOC sample count), `USAGE_MEDIA` output in NORMAL mode (voice-comm → -889; comms-mode ducks media to 1%), guest play_buf FIFO + render-independent bg-tick pump (~60/s), earpiece routing, over TURN, ~20% CPU. Bidirectional signaling reliable (`select_biased!` for inbound Hangup/Offer); ring lifecycle decoupled from comms mode (stops on answer/decline/end). Crypto + TURN ruled out as bottlenecks (`repros/crypto-bench`). 🟡 **Remaining → task 76 + P-list:** inbound accept→connect (answerer ICE), mic/full-duplex TX, diagnostic/flag cleanup. See `[[project_call_audio_output]]`. |
| 76 | `tasks/76-audio-subsystem-refactor.md` | Audio subsystem ground-up refactor (capability-driven). Task 75 shipped call audio but on a hard-coded, guess-driven `audio_impl.rs` (magic `USAGE_MEDIA`, earpiece `deviceIds=[2]`, `WANDR_EARPIECE`/`COMMS_MODE` hacks, no device model). This task: enumerate audio capabilities from the binder services (`media.audio_policy` `listAudioPorts`/`getDevicesForAttributes`/`setForceUse`, `media.aaudio`, `IAudioService` volume, `IVibrator`), build a real device/routing/channel model, map the full state matrix (usage × phone-mode × device × sharing × format × direction → WORKS/FAILS), wire volume + keys, and replace the hacks with a capability-driven routing core + clean WIT. Seeded with task-75 WORKS/FAILS findings + internet refs (AAudio device-id semantics, IAudioPolicyService AIDL, AudioDeviceInfo, AOSP audio-policy, Oboe). 🔲 scoped, not started — fresh session. |
| 77 | `tasks/77-wandr-arbiter-sensors.md` | `wandr-arbiter-sensors` — the arbiter's SensorService module, scoped FIRST so future sensor consumers (proximity-screen-off-during-call, accel→orientation, light→auto-brightness) plug in by reacting to a sensor event instead of patching ad-hoc HAL access. Today the arbiter reads no sensor HAL (orientation enters via the `report-orientation` push). Design mirrors the binary's screen-poller/alarm-timer precedent: a binary-side `ISensorManager` (rsbinder) driver thread `bus_emit`s `Event::SensorReading`; the pure module ref-counts enable-on-demand (battery), translates raw→semantic (`Event::ProximityChanged` w/ debounce), and exposes a `report-sensor` SIM verb (desktop-testable before the HAL). Reference: host `sensors_impl.rs` (AIDL+channel). Closes with the service + one consumer proof (`wandr-arbiter-power` ref-counts proximity on `CommsActive`); the actual screen-off policy + host panel-blank applier are a deliberate follow-on. ✅ DONE + device-verified (Pixel 2 XL, 2026-06-04): shared `wandr-hal-sensors` crate (rsbinder `ISensorManager`, adopted by both arbiter + host) + sensor-driver thread + pure module; real cover/uncover → clean debounced near/far; HAL enable/disable on demand (battery contract, vendor ASH logs); no AVC; threshold derived from `max_range`. 23 unit tests. |
| 78 | `tasks/78-proximity-screen-off.md` | Proximity screen-off during calls — the task-77 follow-on (the deferred panel-blank applier). `wandr-arbiter-power` reacts to `Event::ProximityChanged` while a call is active → SurfaceFlinger `setPowerMode(token, OFF/ON)` via a new shared `wandr-hal-display` crate (full un-trimmed `ISurfaceComposer` codegen + `gui_aidl_types_rs` shim for the native-backed types; float-literal fix in OUT_DIR, no vendored-file mutation). Mechanism de-risked on-device first (transaction code 8, root permission proven via the shared `getPhysicalDisplayToken` gate). Three fail-safes guarantee the panel is always restored (uncover / call-end / call-host death). ✅ DONE + device-verified (Pixel 2 XL, 2026-06-04): cover→OFF, uncover→ON (visually confirmed), hang-up-while-covered→ON; `setPowerMode applied=true`. 9 power unit tests. Out of scope: touch suppression, PM contention, auto-brightness. |
| 79 | `tasks/79-touch-suppression.md` | Touch suppression during a proximity blank — the task-78 follow-on (cheek/ear can't tap mute/hang-up while the screen is off at the ear). Host gate: a `TOUCH_SUPPRESSED` atomic in `input.rs` makes `dispatch_pointer*` (the single touch choke point) drop touch; `ime_inbound.rs` parses an `input-suppress <0\|1>` control line; `wandr-arbiter-power` folds the suppress fan into `set_panel_blanked` so it rides the exact same blank trigger + 3 fail-safes (never stuck on). Touch-only — hardware keys stay live. Reuses the existing per-host push plumbing (no new crate, no core change). ✅ DONE + device-verified (Pixel 2 XL, 2026-06-04): cover→fan to all 6 hosts + injected tap dropped, uncover→resumed, call-end-while-covered→resumed. 10 power unit tests. |
| 80 | `tasks/80-standalone-input-art-less.md` | Standalone input source (ART-less input) — the hard blocker for running the wandr stack with the Android Java framework fully off. Device experiment (2026-06-04) proved `adb shell stop` halts ART (system_server + zygotes) while the whole wandr stack + adbd + native services survive — EXCEPT input: `InputDispatcher`/`InputManagerService` are hosted *inside* system_server (no separate process), so touch/keys die with ART (an InputDispatcher AIDL doesn't help — it dies too). Need our own input: lean **evdev-direct** (read `/dev/input/event*`, decode MT, feed the task-79 `dispatch_pointer_v2`/`dispatch_android_key` choke points + arbiter focus model). Pairs with a `run-hybrid-stack --no-art` deploy mode (targeted `stop zygote`+`zygote_secondary`, keep surfaceflinger/audioserver/sensorservice); dissolves the PMS-contention follow-on for free. See `[[project_art_shutdown]]`. 🟢 core PROVEN+device-verified (2026-06-04): Option B (reuse C++ InputReader) — spike + sf_surface.cpp shim integration + run-hybrid-stack --no-art harness; capstone = swipe-unlock with system_server fully stopped. All 4 steps DONE+device-verified ART-off (per-host input-region filtering: app-area tap no leak; taskbar tap → go-home; keyguard modal). |
| 81 | `tasks/81-artless-display-power.md` | ART-less display power ownership. Found in `--no-art` human testing: the device wedged (black screen, power button + touch dead) because with the Java framework off there's no PowerManagerService to keep the panel powered / wake it on the power key / feed `debug.tracing.screen_state`. Fix: the wandr stack owns display power — host intercepts KEYCODE_POWER → arbiter toggles `SetDisplayPower` (task 78 `wandr-hal-display`); arbiter drives screen state from its own `panel_on` (gating the stale sysprop poller under `WANDR_NO_ART`); force panel on at boot. 🔲 in progress. |
| 82 | `tasks/82-key-input-routing.md` | Key input routing/dedup (ART-less). Found in `--no-art` testing: task-80 Step-2 routed touch but not keys, so every host's InputReader reads hardware keys → one VOLUME-UP press fanned to 6 pids (volume stepped ~6×). Fix: handle each key once (arbiter-side dedup for volume/power first cut; host-side key-focus gating as the proper follow-on). 🔲 scoped. |
| 83 | `tasks/83-artless-security-context.md` | ART-less security context — the shared prerequisite for ALL ART-off privileged native work (display power, standalone inputflinger). The inputflinger spike proved AOSP `InputManager` runs standalone but needs system_server's context: **uid system + gid input + CAP_BLOCK_SUSPEND + a sepolicy domain** (bare root hangs on SurfaceFlinger's ACCESS_SURFACE_FLINGER check + aborts in EventHub). Same context task 81 setPowerMode needs. Decision: dev stand-in now (a setuid+caps `wandr-launch` launcher + `setenforce 0`), flashable-image form DEFERRED. 🟢 dev stand-in DONE+device-verified: tools/wandr-launch (NDK C; uid system + gid input + CAP_BLOCK_SUSPEND) — ran the path-A inputflinger spike through it (framework stopped, setenforce 0) → addService(inputflinger)=0 + InputManager.start=0 + EventHub all devices + touchscreen→display0; PROVES path A (real InputManager runs standalone as the inputflinger service). Remaining: run-hybrid-stack --no-art wiring + image init.rc/sepolicy (deferred). |
| 87 | `tasks/87-artless-audio-output.md` | ART-off audio output — make sound actually play under `--no-art`. Multi-layer onion: each layer is a thing `system_server` (Java `AudioService`) normally does for the native audioserver/AAudio MMAP path that's missing when ART is off (same host code works under ART). ✅ Layer 2 (native stream-volume init `audio_policy_impl::init_audio_policy` — range `-1`→`[0..15]`) + ✅ Layer 3 (`scheduling_policy` binder stub in `wandr-activityms` — kills the `requestPriority` infinite-loop; stream now reaches `STARTED`) DONE+device-verified. ✅ **Layer 4 SOLVED + user-confirmed AUDIBLE:** the wedge was a 4th missing system_server binder — `permission` (`IPermissionController`). `MmapThread::start` → `checkAttributionSourcePackage` → `PermissionController::getService()` loops `checkService("permission") + sleep(1)` for 10s on the audioserver command thread inside `START_CLIENT` → host `startStream` (3s timeout) gives up → no PCM. Fix = `{"permission","android.os.IPermissionController"}` stub in `wandr-activityms` generics[] (built a-03). Found via the ART-up vs `--no-art` A/B; ruled out EXCLUSIVE sharing, `systemReady`, "no volume callback" (appears under ART-up too). Commit `beba3647`. Full detail: task doc + `[[project-artless-audio]]`. |
| 88 | `tasks/88-connectivity-service.md` | wandr connectivity service (ART-off networking, productized). Turns the `--no-art` WiFi+internet live demo (`[[project_artless_network]]`: ping 8.8.8.8 0% loss, TCP 443 open) into a first-class subsystem. The wandr triad: `wandr-hal-net` (rsbinder clients to the surviving native binders `ISupplicant`/`INetd`/`IDnsResolver`/`IWifi`/`wificond`/`rild` + a bundled DHCPv4 client) + `wandr-arbiter-net` (the ConnectivityService brain: transport registry/enumerate, network-selection policy + failover, default-route/DNS, captive-portal validation, metered, auto-reconnect) + `wandr:connectivity` WIT (status/enumerate/connect + `on-change` callback). Key design: `connectivity`/`wifi` are DEAD system_server binders but the data path is kernel↔netd, and guests use WASI sockets → kernel directly, so we drive the native survivors and DON'T reimplement `IConnectivityManager`. Per-transport iface (wifi/ethernet/cellular/bt-PAN): availability/scan/connect/disconnect/status/events. Phasing M1 wifi-STA → M2 ethernet+policy → M3 cellular → M4 BT/SoftAP/VPN. 🚧 **M1 — full IP + DNS DEVICE-VERIFIED + productized (`--no-art`).** netd over binder as uid **system** (not root — the unlock) solves DNS: `wandr-net` (root: associate + pure-Rust DHCP + `ip addr`) re-execs `su 1000 --netd-config` to drive `INetd` (create/addInterface/addRoute/setDefault) + `IDnsResolver` (setResolverConfiguration) over binder, + one catch-all `ip rule lookup <iface>` for the per-UID-fwmark gap. Both AIDLs vendored as submodules (Connectivity/DnsResolver). Daemon auto → ping 0%, `http://example.com` 200, `https://codeberg.org` 200 (DNS+TLS). Earlier `ndc`/M1b notes below are superseded. Thinnest slice built + verified: `wandr-hal-net` (pure-Rust — DHCPv4 client [the one missing native binary, unit-tested] + `wpa_supplicant` ctrl-socket associate + `ip` addr/route applier + WifiConfigStore cred parse; no binder), `wandr-net` daemon, `wandr-arbiter-net` module (5 tests, wired 1-line), `wit/connectivity.wit`. **Device (Pixel 2 XL, live --no-art): `wandr-net` auto-brought-up WiFi — associate `zah004` → pure-Rust DHCP lease `192.168.1.179/22` → route → ping 8.8.8.8 3/3 0% loss**, zero manual steps. On-device fixes baked in: `cleanup_stale` (kill leaked supplicants — `std::process::Child` drop doesn't kill! — + rm stale ctrl socket) + connect-retry-on-`ECONNREFUSED`+PING; **routing gotcha** — Android's `ip rule`s never consult `main` (CS dead) so a default route there is ignored → `ip rule pref 15000 from all lookup main` + default `onlink`. **DNS=M1b (diagnosed):** A15 bionic always uses netd dnsproxyd (`ANDROID_DNS_MODE=local` dead, resolv.conf RO, `net.dns*` ignored); netd `createPhysicalNetwork` permission-rejected even as root (task-87 `IPermissionController` gap). M1b: (a) permission stub→`INetd`+`IDnsResolver` privileged, or (b) custom UDP resolver in wandr-host `ip-name-lookup`. Also M1b: `wandr:connectivity` host bindgen + test guest. M2: cold chip power-up via IWifi HAL. |
| 89 | `tasks/89-artless-sensors-reliability.md` | ART-off sensors reliability + panel_on sync (auto-brightness follow-on to 85/86/81). Opened after a user doubt "auto-brightness doesn't work" → live cover/uncover verify PROVED the mechanism is correct (covered→lux 0→backlight ~10-16; uncovered→lux 5→backlight ~63-67) but the EXPERIENCE is flaky. **Issue 1 (primary):** `wandr-sensors` `SIGABRT` on sensors-HAL `DEAD_OBJECT` — unchecked HIDL `Return<>` in `wandr_sensors_poll` (`libwandr_sensors_hal.so`) aborts the daemon when the HAL connection drops (HAL churn / ART cycles), with NO auto-restart → all sensors (light/proximity/rotation) die. Fix: check the `Return` status in the C++ shim (a-03) + re-acquire the HAL in Rust + respawn in run-hybrid. **Issue 2:** arbiter `panel_on` not synced to power-button wake (light sensor is panel-on-ref-counted, so a button wake alone doesn't re-enable it until the arbiter cmd runs ~1s later). **Issue 3:** screen idles off too fast + coarse light sensor (0/5 indoors). Tombstones 27-41. ✅ Issue 1 DONE+device-verified+pushed (96a58117: HIDL Return-check in the a-03 shim + reconnect loop + respawn). ⛔ Issues 2+3 REASSESSED as NOT real bugs: power-key path works end-to-end (verified `power-key` toggles panel + applies backlight 0↔91; the "never reaches arbiter" was a logging artifact — arbiter log::info isn't captured in wandr-arbiter.log); screen-timeout is a reasonable 60s. The user's symptom was entirely Issue 1 (dead wandr-sensors → no light feed). Net: 🟢 task effectively DONE (the real bug fixed); only a clean real-world re-confirm + the optional arbiter-logfile fix remain. |
| 90 | `tasks/90-connectivity-wit-implementation.md` | Implement the `wandr:connectivity` WIT (guest-facing connectivity) — spun out of task 88 so we can return to task 87 (ART-off audio bugs) first. The connectivity *subsystem* is built + device-verified (88: `IWifi`→`ISupplicant`→DHCP→`INetd`→`IDnsResolver` over binder as uid system) and the WIT *contract* is drafted+validated (`wit/connectivity.wit`, `6a36f2b1`), but it's NOT wired into wandr-host — no guest can use it yet. Scope: M1 = `network` status wiring (bindgen `connectivity-host`/`-events`, `get-status`→arbiter `net-status`, deliver `net-changed`→guest `on-connectivity-change`; mirror `wandr:alarm`); M2 = `wifi` scan (bind `IWificond`) + connect (generalize built `supplicant_hal::associate`) + radio (built `wifi_chip`), privilege-gated; M3 = saved-network store + auto-connect (needs credential-storage decision: wandr-owned vs `WifiConfigStore.xml`); M4 = manual static IP + `wandr.settings.wifi` chrome. Open: credential storage, `wifi` privilege model, `IWificond` scan-as-system. 🔲 scoped. |
| 91 | `tasks/91-artless-call-audio.md` | ART-off **call** audio — two-way voice in a wandr Signal call under `--no-art`. Symptom: call rings (output works, task 87) but the call has **no speech in/out**. Distinct from task 87 (output-only) + task 75 (call output under ART): a call is **full-duplex** (mic-capture + playback simultaneously), and the Pixel 2 XL can't hold an MMAP input + MMAP output at once (`-889`). Grounded in current code (`call.rs::pump_audio` opens OUTPUT-first as USAGE_MEDIA→legacy-SHARED so the capture MMAP coexists). **Prime suspect (H1, the task-87 irony):** task 87 made MMAP *output* work under `--no-art`, which may push the call's SHARED output onto exclusive MMAP → it holds an MMAP output → mic-capture `-889`s → no TX (and possibly no RX). Diagnosis = `wandr-host --probe-audio-loopback` (coexistence: "LEGACY/coexists ✓" vs "MMAP-spin ✗") + ART-vs-`--no-art` call A/B. Other hyps: H2 capture-START permission/attribution block (extend task-87 stub to input path), H3 SHARED-mixer `--no-art` block, H4 comms route not applied. 🔲 scoped+grounded (no device test/patch yet). |
| 93 | `tasks/93-video-call.md` | Video calls (AV gap to a full Signal app). ✅ HW codecs BOTH WAYS proven under `--no-art`: Camera CAPTURE reliable (29.1fps raw + HW VP8 encode 17.4fps, 3/3 — task-95 EIS-gyro race won via the batterystats shim, see [[project_artless_sensor_5s_batterystats]]) **AND HW VP8 DECODE now confirmed** (2026-06-10, `wandr-host --probe-video decode` loopback: camera→HW-VP8-encode 20.1fps→HW-VP8-decode 19.9fps, 100/101 frames, decoder `configure()` did NOT block — the last open unknown). Nothing missing at the Android-native layer (cameraserver + media.codec HW Codec2 + gralloc all survive; SoC HW `OMX.qcom.video.{encoder.vp8, decoder.vp8, decoder.vp9}`). WIT contracts **drafted + validated** (`697da5de`): `wit/video.wit` + `wit/crypto.wit`. Audio calls connect both ways (incoming fixed). ✅ **Phase 2 (`wandr:crypto` SRTP HW-AES offload) DONE + LIVE-CALL-VERIFIED** (2026-06-10: real Signal call, audio both ways; 3.0× audio / 8.5× video-size vs in-wasm AES, `11c5efaa..3c316895`). ✅ **Phase 1 (host `wandr:video` impl) DONE + DEVICE-VERIFIED** (2026-06-10): `video_probe.rs` promoted to `video.rs` backend + `video_host_impl.rs` WIT resources (linked onto every guest; lazy post-fork binder threadpool; ordered null-tolerant teardown); WIT gained `camera-facing`, decoder coded-size hint, `queue-full`, `decoded-frames()`; verified by `apps/user/wandr.video.test` `--run-once`: **25 fps camera→HW-encode→guest→HW-decode loopback**, 124/125 decoded, request-keyframe + set-bitrate live, 2× back-to-back clean (no cameraserver wedge). ✅ **Phase 3 (wandr-call video track) DONE + DEVICE-VERIFIED** (2026-06-10): `crates/wandr-call/src/video.rs` VP8 RTP track on the shared SRTP contexts (RFC 7741 fragmentation, torn-frame-safe reassembly) + SRTCP control loop (PLI/FIR in→`take_keyframe_request`, `request_keyframe`+auto-PLI out rate-limited, ~1 Hz video SR, REMB→`peer_remb_bps`; works through the host-AEAD cipher); ringrtc wire constants source-grounded (VP8 **PT 108**, RTX 118, SSRC base 1000/2000 +3 video → 1003/2003); 21/21 engine tests; device capstone `wandr.video.test` Part 2: camera→HW-VP8→PeerSession A→SRTP/real-UDP→B→**88/88 frames 0 broken**→HW decode **17.4 fps**, PLI round-trip answered on-wire. ✅ **Phase 4 (decode-to-surface render + PiP self-view) DONE + DEVICE-VERIFIED** (2026-06-10, screenshot-proven live video on the panel): SurfaceView-model child surfaces (`sf_media_*` shim API: container+BBQ child of the app's surface, remote z=-2 / PiP z=-1 below the hole-punched UI via `sf_set_opaque`; NO arbiter role needed — z/visibility/rotation inherit from the app's role) + encoder PiP (second camera output target) + WIT `preview`/`set-preview-rect`/`set-visible`; gotchas: explicit `Surface*`→`ANativeWindow*` upcast before void* (else camera2ndk SIGSEGV), desk-down back camera = black frames, sensor rotation uncompensated (Phase 5). ✅ **Phase 5 (Signal in-call video) DONE + LIVE-CALL-VERIFIED** (2026-06-10, real Signal peer, video both ways, user-confirmed): video on/off via rtp_data `senderStatus.video_enabled` (ringrtc-grounded, accumulated ~1 Hz), peer `receiverStatus.max_bitrate_bps` → encoder bitrate, CVO rotation ext id 4 both ways (sensor orientation out, decoder `rotation-degrees` in), VP8-only receive advert; engine `pump_video` (lazy enc/dec, keyframe-gated join) + full-screen `VideoCallScreen` UI (camera toggle, surface-derived rects, above-ui layer). **TASK 93 COMPLETE.** Hardening from live calls: RED demux (PT 120), TWCC feedback (mandatory for transport-cc peers), 250k REMB floor, receive-budget adverts, VP8-keyframe aspect-fit, container-matrix rotation (the only producer-proof mechanism — BBQ overwrites layer setTransform per buffer, MediaCodec overwrites producer-window transform), live CVO + device rotation. Follow-ups (cosmetic): mirrored self-view, behind-ui hole, 4-pose landscape face-validation. |
| 94 | `tasks/94-wandr-sensors-refactor-for-sensorservice.md` | Unify sensors on the native AIDL `ISensorManager`; delete `wandr-sensors`. `wandr-sensormanager` publishes BOTH `android.frameworks.sensorservice` transports (HIDL @1.0 for the camera EIS gyro, task 93 + **AIDL** `…ISensorManager/default` for the wandr stack); the arbiter's task-77 sensor-driver consumes the AIDL endpoint via the shared `wandr-hal-sensors` crate (orientation→auto-rotation, proximity, light); the old direct-HAL `wandr-sensors` daemon is deleted (single-client HAL contention gone — `lshal`: only `sensorservice` opens `ISensors`). The original ‼️KNOWN BUG (AIDL event-queue "100% CPU spin") was root-caused on-device — a **stale arbiter binary** held the **gyroscope @200 Hz** (continuous stream, ~200 ev/s × a synchronous `onEvent` binder round-trip), and the poll thread runs **SCHED_FIFO prio 10** so it starved the box; NOT the fakeVM/null-JNIEnv theory (the poll thread blocks on `pollAll(-1)` correctly). Conflict analysis + fixes in `docs/sensor-access-conflicts-no-art.md`: **C1** retire host orientation read so the arbiter is the sole device-orientation consumer (host = pure applier of arbiter `geometry` pushes; handle 0x17 `active-count` 2→1) — ✅ device-verified `9d28ecf2`; **C2** drop `CAP_SYS_NICE` in `wandr-sensormanager` so the poll thread runs SCHED_OTHER (degrades, no whole-system freeze) — ✅ device-verified `3dc5f699`; **C3** `wandr-hal-sensors` `ping_binder`+reconnect+replay-enabled-set so sensors auto-recover after a `wandr-sensormanager` restart (no latched-dead cache) — ✅ device-verified `6da06e7b`; **C4** cross-path HAL perturbation onto the camera EIS gyro (a high-rate AIDL consumer loads the same single-client HAL) — 🔲 OPEN; the residual server-side HANGUP busy-spin guard (vendored `EventQueue.cpp`, needs `libsensorserviceaidl` rebuild + side-load) — deferred, benign post-C2. 🟢 sensor unification + C1/C2/C3 DONE+device-verified; C4 + HANGUP-guard open. |
| 96 | `tasks/96-artless-native-service-bringup.md` | Churn-free, shim-first `--no-art` native-service bringup. Root-causes the whole bringup mess (sensorservice `DEAD_OBJECT`, audioserver wedge+restart cycle, manual sensor recovery, duplicate-instance CPU spin, the C3 EventQueue busy-spin) to ONE cause: framework-coupled native services started in full-framework context then orphaned by stopping the framework → **churn** (restart-to-clean). Two distinct symptom mechanisms (audioserver = `class core`, survives, *wedges* on `waitForService`; sensorservice = hosted in system_server, *dies*, standalone replacement hits the single-client sensors-HAL handoff race → `DEAD_OBJECT`) but one root. Fix = (1) a designed framework-shim serving BEFORE native services start, (2) start each native service ONCE fresh in the post-ART context, (3) one stable arbiter sensor connection (no hangup ⇒ no EventQueue spin). **Supersedes the C3 "patch `aidl/EventQueue.cpp`" idea** — closes by avoidance, **no platform-library patches** (user constraint). Analysis: `docs/artless-native-service-model.md`. ✅ IMPLEMENTED+device-verified 2026-06-07/08: `wandr-framework-shim` (C++ libbinder, retires `wandr-activityms`) + layer-split bringup (`native_shim_healthy` skip-if-healthy, `--wandr-only` fast restart) in `run-hybrid-stack.sh`. Shim service set = activity/sensor_privacy/package_native/processinfo/scheduling_policy/permission/permission_checker/media.camera.proxy + **`batterystats`** (`27f94b4c` — fixed the ~5s sensor/proximity enable stall, `BatteryService::checkService` blocking `getService`; whole-pipeline mLock freeze) + **`appops`** (`f9c1cc1e` — AppOpsManager 10s sleep-loop, typed `AppOpsStub`). The batterystats fix also **resolved task 95** (the EIS-gyro race — gyro-enable paid the same 5s stall). |
| 95 | `tasks/95-camera-noart-eis-gyro-session-race.md` | Camera under `--no-art`: the EIS-gyro startup race (task-93 "28.8 fps" was real but ~1/8 non-deterministic; lose → `msm_stopGyroThread: invalid timer state = 0` → 0 frames). Long debug ruled out Magisk am-spin (separate, fixed via `adb root`), provider-degraded, JVM nullptr-vs-fakeVM, binder threadpool, SLPI churn, SELinux; the persistent-gyro-client hypothesis was **built + FALSIFIED** (0/12 — raw gyro `0x04` ≠ camera-sync `0x09`; failure is downstream in the kernel OIS hrtimer). ✅ RESOLVED 2026-06-08 by the task-96 **`batterystats`** shim stub: the camera's EIS gyro-enable was paying the same ~5s `BatteryService::checkService` blocking-`getService` stall, which perturbed the downstream `ois_open → msm_startGyroThread` timing; registering `batterystats` → gyro session establishes promptly → OIS arms → frames flow. Verified **3/3** across raw/SW/HW VP8 with no gyro warming (≈0.2% by chance at the 1/8 baseline). "Battery saves gyro." |
| 97 | `tasks/97-artless-call-audio-stall.md` | ART-off call-audio stalls + audioserver instability (spun out of task 91). **Bug #1 (output stream STALLS — `wr_ok` frozen, silent):** ROOT-CAUSED (source + device A/B via new `--probe-call-stall`) = a SHARED output that underflows gets an UNTHROTTLED `XRUN` service event every mixer burst (`AAudioServiceEndpointPlay::callbackLoop`→`incrementXRunCount`); the 128-deep up-message FIFO overflows if the client stops draining ~0.5s → `setSuspended(true)` → the mixer skips the stream (`continue; // dead stream`) → client FIFO read counter freezes → silence. Self-recovers iff the guest keeps draining (every `write_pcm_f32` drains) ⟹ **bug #1 ⟺ bug #3** (permanent only when the guest call loop falls idle). FIX = host call-output silence-pump (`spawn_call_silence_pump`), but on a LIVE call it interfered with the guest write-then-start handshake → **shelved to probe-only** (`--probe-call-stall … 1`). **Bug #5 (earpiece/speaker toggle `-889`):** ROOT-CAUSED = the per-stream `deviceIds` pin makes AAudio open a SECOND MMAP "direct output" but `mmap_no_irq_out` is `maxOpenCount=1` → earpiece pin `-889`s when another output holds the slot; speaker only "worked" by reusing the existing MMAP. FIXED+device-verified = drop the call deviceId pin (`Route::Call → device_ids=[]`, shares the MMAP, never -889) + route by re-routing the shared output via `setDevicesRoleForStrategy(MEDIA, PREFERRED, [OUT_SPEAKER\|OUT_SPEAKER_EARPIECE])` from `set_comms_route` (mid-call, no reopen; cleared on call-end). Verified over binder (`--probe-route-toggle`: route followed 140→141→140). Also fixed `start()`/`pause()` holding `track_map` across the blocking binder call. Commits `069bcc3d`/`4fa32cdb`/`d523dd96`/`39196f50`. 🟡 **Silent call PERSISTS even de-risked** → traced to a DEEPER pre-existing blocker: AAudioService command-queue timeouts (`startStream -885`, `Command N time out` START/STOP/CLOSE, standby) — the whole AAudioService control plane is flaky under `--no-art`. → **superseded by task 98** (replace the AAudio backend with AudioFlinger-direct). |
| 98 | `tasks/98-wandr-audio-audioflinger-backend.md` | `wandr-audio` — replace the hand-rolled AAudio (`media.aaudio`) backend with an **AudioFlinger-direct** one. Decided after task 97 proved AAudioService is the `--no-art`-broken layer (`-885`/command-queue timeouts/standby) AND the reframing that this is a **general audio framework** (music/"Spotify-like"/notifications/games/calls), whose universal native base is `AudioTrack`/`AudioRecord`→AudioFlinger (what ExoPlayer/MediaPlayer/SoundPool/telephony all use; AAudio is the low-latency niche). Survey ruled out every off-the-shelf crate: `cpal`/`tinyaudio`/`oboe`/`aaudio`→AAudio (broken layer; tinyaudio output-only; cpal too generic), `android-media`→JNI→Java AudioTrack (**needs ART**). No crate wraps native `libaudioclient` (private C++ lib, no stable ABI). **Decision = Route R:** rsbinder → `IAudioFlingerService.createTrack` → `IAudioTrack`/`IAudioRecord` (control plane, like the existing `IAudioPolicyService`; AudioFlinger's `start()` is direct → **deletes the AAudioService bug class**) + a Rust port of the `audio_track_cblk_t` `ClientProxy` ring (data plane; faithful port from vendored `AudioTrackShared.{h,cpp}`; non-blocking writer skips the futex). **Fallback S:** C++ `libwandr_audio.so` shim over native `AudioTrack` (libgui/`libsf_surface` pattern, reuses `AudioTrackShared.cpp`, C++ ABI cost). Routing/volume stay on `IAudioPolicyService`; WIT/guest unchanged (only the host backend swaps); `StreamClass`→`audio_attributes` per consumer. **Backend = standalone DECOUPLED repo https://codeberg.org/harryzz/audioclient-rs** (pkg `audioclient-rs`, lib `audioclient`; self-contained — vendors its own AIDL closure under `aidl/`; cfg-android with desktop no-op; consumed by `wandr-host` as a Cargo git-dep, which keeps only the WIT/wasmtime glue + `StreamClass`→attributes). Reuse caveats: Android-native only, version-pinned ABI (`audio_track_cblk_t`/AIDL layout), privileged context. 🟢 **Step 1 DONE** (2026-06-08): crate scaffolded + decoupled + pushed; `IAudioFlingerService` closure codegens AND compiles for aarch64-android (verified standalone), one `AudioHalCapRule` recursive-parcelable build.rs stub. host keeps `IAudioPolicyService`, drops `IAAudioService`/`IAAudioClient` + hand-rolled client as later cleanup. Next: host git-dep + the `audio_track_cblk_t` proxy (steps 2–4). 📐 PLAN OF RECORD. |
| 100 | `tasks/100-slint-renderer.md` | **slint-wandr** — Slint renderer over `my:skiko-gfx` (evaluation track; dioxus-canvas stays production until M4 proves the DSL earns it). Groundwork all landed 2026-06-11: `docs/skia-wit-mapping.md` (canonical Skia↔WIT contract from the 3-consumer union: Compose/skiko=all verbs, dioxus-canvas subset, Slint renderer-skia inventory) + the additive 6-verb batch (`create-typeface`/`drop-typeface`, `draw-glyphs`+bc, `draw-shadow-rrect`+bc) implemented in wandr-host `30a61c12` (built, NOT yet deployed — M4 is the first live exercise). De-risked: Slint compiles for wasip2 (std+compat-1-2); the parley text stack (i-slint-core std,shared-parley) compiles for wasip2 → text shapes GUEST-side, renderer gets font-bytes+glyph-ids (`GlyphRenderer`), metrics = shared `sharedparley::*` helpers; plug point = `RendererSealed`+`ItemRenderer` (femtovg template). Pin: Slint git `46cfde65` (1.17-dev — parley switch unreleased; i-slint-* semver-unstable). M1 crate skeleton (Platform/WindowAdapter/event wiring) → M2 ItemRenderer over canvas verbs (layers via bc-*) → M3 glyph text → M4 `wandr.slint.test` on device (visual verify with user) → M5 IME/lifecycle if earned. ✅ **COMPLETE + USER-VERIFIED** (2026-06-11): all milestones done in one day — renders + fully interactive (touch/typing/selection/scroll/animation) + IME summon/dismiss (hide-button ESC convention + tap-outside) + emoji (device NotoColorEmoji → fontique GenericFamily::Emoji) + multiline TextEdit. Gotchas: detect_operating_system browser-trap (OPERATING_SYSTEM_OVERRIDE→Android), generate! inline+default-wit collision (path:[]). ~0.7% idle CPU. Adopt-vs-dioxus decision open. |
| 101 | `tasks/101-desktop-dev-loop.md` | **Desktop dev loop + W3C key-input.** ✅ DONE + USER-VERIFIED (2026-06-11): the x86_64 host runs the SAME guest wasm as the device, JIT, in a winit window (WSLg-verified live) — `WANDR_DESKTOP_SIZE=WxH wasm-android-host <ui.wasm>`; render parity (Slint demo + dioxus differential), mouse, HW keyboard, live resize, color emoji. Pieces: (1) `my:skiko-gfx/key-input` v3 — W3C UIEvents code token (string; winit KeyCode Debug names = the tokens) + alt/ctrl/meta/shift + text + repeat, probe-only world (frame-pacing pattern, additive), emitted v1+v2+v3 from winit/InputFlinger/IME, slint-wandr consumes with v2 fallback — HW-keyboard user-verified; (2) desktop build un-rotted (task-93 aarch64 cpufeatures + ungated android refs); (3) ‼️ present path was MISSING entirely (flush_and_swap android-only → black windows forever) → softbuffer blit; (4) ‼️ on-resize wasn't dispatched to guests — ONE gap masquerading as THREE render bugs (gradient/slider-track/ListView "missing" = stale layout size; differential-guest localization); (5) WANDR_DESKTOP_SIZE env; (6) noto dir preopened as /system-fonts (emoji parity, static-emoji probe). Edges: no arbiter (IME warns, insets 0), scroll-wheel unmapped. Recipe: docs/build-pipeline.md. |
