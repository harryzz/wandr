---
name: wit-bindgen-no-kotlin-generator
description: "Kotlin WIT bindings: USE github.com/Kotlin/wit-bindgen branch `kotlin` (validated end-to-end on wasi:canvas 2026-06-11 — resources, spilled records, strings, list<record> all correct); upstream wit-bindgen CLI has no Kotlin output; skiko's \"Generated by wit-bindgen 0.53.1\" header = that same JetBrains fork's ancestor"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: d9451151-9116-4c95-a45d-8758673104ce
---

`wit-bindgen --help` on `cargo install wit-bindgen-cli --version 0.53.1` lists only: rust, c, cpp, go, csharp, moonbit, markdown, test. **No `kotlin` subcommand.**

`skiko/skiko/src/wasmWasiMain/kotlin/generated/InternalSkikoUi.kt` and `ComponentSupport.kt` say "Generated by wit-bindgen 0.53.1. DO NOT EDIT!" at the top. This is misleading — those bindings come from a **private JetBrains fork** of wit-bindgen, not the upstream cargo-installable version. Confirmed by `find / -name 'wit-bindgen*kotlin*' -type f` returning empty.

**Why:** task 36 step 6 wanted to auto-generate Kotlin bindings for `wandr:markdown/renderer@0.1.0` for the smoke consumer. Hit this dead end after installing wit-bindgen-cli (4-min compile, wasted time). Ended up hand-writing bindings using skiko's structure as a model.

**How to apply:**

- For new Kotlin/Wasm WIT bindings, hand-write following skiko's pattern: `@WasmImport(interface, name)` external decls + canonical-ABI marshaling helpers (writeBytes, return-area allocation, etc.).
- Kotlin stdlib's `componentModelRealloc` covers the `cabi_realloc` export: `@WasmExport fun cabi_realloc(ptr: Int, oldSize: Int, align: Int, newSize: Int) = componentModelRealloc(ptr, oldSize, newSize)`. Requires `@OptIn(ComponentModelInternalApi::class)`.
- For complex return types the import-side canonical ABI differs from the export side — see [[canonical-abi-import-export-asymmetry]].
- The JetBrains fork IS public: **`github.com/Kotlin/wit-bindgen` branch `kotlin`** (`wit-bindgen kotlin …`), used by `Kotlin/sample-wasi-http-kotlin`. **VALIDATED END-TO-END 2026-06-11 at rev `6b9cb12`** by the task-102 spike (`apps/user/wandr.ktcanvas.test` — full wasi:canvas import + my:skiko-gfx/renderer export, desktop + Pixel-2-XL-verified, zero hand-written fallbacks): imported RESOURCES work (own/borrow, `[resource-drop]`, AutoCloseable `close()`, consuming statics zero the handle — don't `close()` a consumed builder, that drops handle 0); >16-flat-arg calls spill into a single computed memory blob (correct offsets incl. option<record> + borrow-in-record); strings, list<tuple>, list<record> returns all correct. Memory discipline is EMITTED by the generator: freeAll at export entry (crates/kotlin/src/lib.rs:1593) + after every import CallWasm (lib.rs:2417); post-call-freeAll-then-lift is safe because lifts only read linear memory into GC objects. Generated `runtime/ComponentSupport.kt` provides `cabi_realloc` — don't hand-define a second one. Invocation: `wit-bindgen kotlin --kotlin-imports 'impl.*' -w <world> wit --out-dir src/wasmWasiMain/kotlin/bindings/`; implement exports as `object <Iface>Impl : bindings.<Iface>` in package `impl` (NOT --generate-stubs into bindings/ — regen would clash). Still unimplemented (don't matter for canvas): futures/streams/maps/async, fixed-len lists, flags >64 bits, `DropHandle` (lists of handles). Bindings structure "not final" — pin the rev.

Working hand-written example: `wandr-app-md-smoke/src/wasmWasiMain/kotlin/generated/MarkdownImports.kt` — covers just the `render` call (string in, document return-area, read blocks.len). Generalizing to full document tree traversal is significantly more work.

Related: [[canonical-abi-import-export-asymmetry]], [[task-36-step-7-pending]].
