# Memory Index

## ‼️ IMPORTANT STANDING RULES (read every session)
- [RULE: do NOT hardcode](feedback_no_hardcoding.md) — derive values from first principles / real runtime inputs (screen geometry, density, row counts), not magic numbers; keep it resolution/device-independent; a genuinely-needed constant must be ONE named, justified source of truth in the layer that owns the policy. (User called this out repeatedly — task 71: keyboard 864/640, MIN_CONTENT 500, ×11/10. 2026-05-31.)
- [RULE: build-system-warpkgs.sh WIPES the apps root](feedback_build_system_warpkgs_wipes_apps_root.md) — it `rm -rf $APPS_ROOT`, destroying ALL installed apps + runtime state/ (Signal link+history). NEVER run on a device with live user state; rebuild one app via per-app `wart-host --install`. Don't do unprompted hygiene rebuilds. Don't give unproven answers (verify behaviour, not file existence). (Destroyed user's Signal chats, task 71, 2026-05-31.)
- [RULE: clean library usage](feedback_clean_library_usage.md) — consumer/app code depends ONLY on the library's public API + its own logic; never leak backend/WIT/codegen plumbing into the app (keep it portable across targets). If something must leave the lib's scope, ASK FIRST. (From the dioxus-guest mess, 2026-05-30.)
- [RULE: always use latest versions](feedback_check_latest_versions.md) — before using any crate/lib/component, check the LATEST version online and use it; don't reuse stale pins or read stale local-cache copies. (Used wit-bindgen 0.46 / read stale 0.57.0 vs current 0.57.1, 2026-05-30.)

- [Project: WASM Android Runtime](project_wasm_runtime.md) — goals, stack, current status
- [Project: keyboard/overlay lifecycle (task 71)](project_keyboard_overlay_lifecycle.md) — IME sizing DERIVED (intrinsic dp × density, MAX_SCREEN_FRACTION=0.45 landscape cap, host=pure applier); overlay visibility = derived state (reconcile_overlay) + focus-follows-foreground (Android model: switch hides keyboard, re-tap shows); dioxus-canvas clamp fix (rotation crash); `present` push redraws on show. Device-verified 2026-06-01, UNCOMMITTED.
- [Project: arbiter-as-window-server design](project_arbiter_window_server_design.md) — design of record (docs/visual-sizing-design-patterns.md): arbiter = native system_server-coordinator (AMS+WMS+IMMS, decides-never-renders), modular core+crates, per-display + generalized resource-focus bake-ins. Modular core + first WMS slice built in task 73.
- [Project: task 73 modular arbiter + WM](project_task73_modular_arbiter_wm.md) — wart-arbiter→workspace (core kernel + wart-arbiter-wm); WMS geometry (insets/keyboard/orientation) moved host→arbiter via one `geometry` wire push; host=dumb applier; orientation report-up+fallback. Device-verified + committed (d078ab88).
- [Project: task 74 surface/role model](project_task74_surface_role_model.md) — Increment 2: core per-display surface/role + generalized resource-focus model (the load-bearing schema); dissolves the overlay↔foreground knot. A+B+C-write-through committed (9b21c035, d7962f6c) + device-verified (zero drift); model is the live single source driving reads. D (delete legacy dual-state) + shell-module extraction + E (WM cache) remain — details in file.
- [Project: Signal app location](project_signal_app_location.md) — Signal app promoted to `apps/user/war.signal/` (engine/+ui/+build.sh) 2026-05-31; spikes/smoke stay in repros/
- [Project: Signal resume point](project_signal_resume_point.md) — PAUSED 2026-05-31: shipped feature list + next steps (send-receipts, attachments incl. voice/video; calls=ringrtc, out of scope) + gotchas
- [Project: boot-model libgui build](project_boot_model_libgui_build.md) — task 33 C++ shims build in-tree on the a-03 AOSP host. a-03 is AVAILABLE INFRASTRUCTURE (128GB/72core, ssh-reachable), NOT a blocker — using it is the normal workflow, don't defer tasks "because they need a-03". Also check the Rust/Skia path first (e.g. WART_ORIENT does content rotation with no shim).
- [Project: standalone orientation](project_standalone_orientation.md) — task 33 orientation fixed: eglQuerySurface lies on taimen Adreno (transposed dims); use ANativeWindow geometry. It was a buffer-size bug, not a rotation bug.
- [Reference: on-demand rendering / high-CPU fix](reference_on_demand_rendering.md) — high idle CPU root cause: every guest renders 60fps unconditionally (game-loop). Fix = optional `frame-pacing` WIT (next-frame-delay→u32), host gates render but cheap-polls input. **Steps 1-3 DONE+device-verified 2026-05-30** (host dirty-gate + launcher/statusbar/taskbar + dioxus; idle CPU launcher 23→5/statusbar 20→8/taskbar 15→6/dioxus 55→6%). Compose/skiko = step 4, deferred (heavy republish). Statusbar trap: move clock refresh off frame%N to per-render.
- [Feedback: visual verification needs the user](feedback_visual_verification.md) — for subjective/visual outcomes (scroll smoothness, animation, fps feel) set up the test + ask the user to inspect; don't claim "verified" from CPU/logs (mechanism active ≠ result good). Default risky knobs conservatively. From task 64 fps-cap: 30fps scroll felt choppy though CPU dropped.
- [Reference: compose-wasi consumption](reference_compose_wasi_consumption.md) — Compose guests depend on in-tree `org.jetbrains.compose.*:*-wasm-wasi:9999.0.0-SNAPSHOT` modules (BUILD-wasmWasi.md Step 4); NOT the discarded out-of-tree `compose-*-wasi:0.0.0-wasi-local` fat bundles. wart-app/IME were on the stale bundles → in-tree edits didn't reach them (2026-05-30 fix). Step 5: republish one module after an additive edit.
- [Reference: warpkg manifest convention](reference_warpkg_manifest_convention.md) — each warpkg's package.toml lives in its app source dir (apps/.../package.toml); pack scripts copy it, no heredocs. Edit the file directly. orientation field: launcher=locked, rest=auto.
- [Project: overlay orientation](project_overlay_orientation.md) — task 62: IME/statusbar/taskbar rotate. Generic `orientation=auto` manifest flag; anchor-aware overlay_rect (handedness 0→S/3→N/4→W/7→E); a-03 shim sf_set_overlay_geometry+sf_panel_dims; Compose wasi guests need the task-43 delegate w/h resize fix; redeploy with `pkill -x` not `-f`. Future: app-lock propagates to chrome via arbiter.
- [Project: standalone input](project_standalone_input.md) — task 33 Step 3 (InputFlinger input) — RESOLVED 2026-05-22, device-verified: touch routes, scrolling works. Geometry fixed via BBQ-direct attach to g_control.
- [Project: standalone keys](project_standalone_keys.md) — RESOLVED 2026-05-26: hardware keys wired into standalone loop via SfInputEvent kind=10/11 + dispatch_android_key; periodic sf_request_focus() needed because activity-backed windows steal InputDispatcher focus from non-Activity wart.
- [Project: IME options (no-Java)](project_ime_options.md) — Android IME is binder-based on the wire (Java only the façade); rsbinder→IMMS is possible but high churn. Standing decision: polish in-canvas Compose keyboard (A); rsbinder real-IME (B) on roadmap not next; wasi-guest IME (C) is the post-ART north star.
- [Project: app lifecycle + packaging](project_app_lifecycle_and_packaging.md) — post-art-roadmap §7+§9 distilled. §9 RESOLVED 2026-05-26: monolithic now / Hybrid (zygote) target; SF + InputFlinger kept; APK ecosystem out of scope; per-component caps via link.wac. **Empirical update 2026-05-27 (task 45 spike):** wart-zygote works end-to-end on device; Adreno EGL survives fork; per-app working set ~180 MB dominates COW savings (~5.6 MB/child) — Hybrid win is isolation not memory. Production work spun out as recommended task 46. Q5b (signing) + Q6 (same-Store vs separate-Store composition default) still open.
- [Feedback: wart-zygote fork survival](feedback_wart_zygote_fork_survival.md) — empirical (task 45 spike): what's COW-safe parent→child on this stack, where Engine preload savings actually come from (mostly binary .data/.bss, not engine heap), per-app working-set scaling (~180 MB).
- [Feedback: no ART-layer dependencies](feedback_no_art_layer_dependencies.md) — don't build wart features on system_server/WindowManager/ActivityManager/launcher (being dropped post-ART); use HAL/binder signals (sensors HAL, SurfaceFlinger, InputFlinger) that survive. Trap: dumpsys window mCurrentRotation is activity-driven and useless when wart owns the screen — read the accelerometer via the rsbinder sensors HAL instead.
- [Feedback: arbiter death notification](feedback_arbiter_death_notification.md) — task 54: zygote (not arbiter) is the parent of app children, so the arbiter learns of deaths via a SUBSCRIBE_EXITS push bridge + 5s poll backstop; clean up per-app resources in handle_child_exit, not just the kill handlers.
- [Reference: photon image lib](reference_photon_image_lib.md) — image processing (filters/resize/color) for guests: photon-rs fits wasm32-wasip2 (default-features=false; `image` for codec). Probe + how-to: `repros/photon-probe/README.md`
- [Reference: dioxus + taffy Rust UI](reference_dioxus_taffy_rust_ui.md) — dioxus-core + taffy is a viable LIGHT reactive Rust UI framework on wasm32-wasip2 (424KB, no wasm-bindgen) — the Compose-alternative candidate for complex wart guests. SHIPPED as crates/dioxus-canvas/ (task 59, device-verified); demo apps/user/war.dioxus.demo/. NOT full dioxus-native/Blitz (GPU/vello; it DEPENDS on dioxus-html, not an alternative to it). For simple UI, hand-rolled canvas (war.launcher ~70KB) is lighter.
- [Feedback: no posting without authorization](feedback_no_posting_without_authorization.md) — never open PRs/issues, comment, fork, or push to public remotes on the user's behalf until they explicitly authorize that specific action; prepare locally + ask. A general "go ahead" doesn't extend to the public-publish step.
- [Reference: dioxus 0.7 wasip2 / subsecond wall](reference_dioxus_07_wasip2_subsecond.md) — dioxus >=0.7 won't build for wasm32-wasip2 (dioxus-core hard-deps subsecond → wasm-bindgen on all wasm32; not fixable by feature; main not fixed). Clean PROVEN fix: narrow subsecond's cfg to all(target_arch="wasm32",target_os="unknown") + [patch.crates-io]. Don't re-pin 0.6 — adopt task 60.
- [Feedback: bionic_compat fixes](feedback_bionic_compat.md) — NDK linker gotchas and LineageOS crash fixes
- [Feedback: Android EGL/Skia](feedback_egl_skia.md) — EGL context lifecycle and SkiaRenderer drop order
- [Feedback: Android font loading](feedback_android_fonts.md) — FontMgr::default() gives zero-metrics typefaces; must use new_from_data from TTF bytes
- [Feedback: wasmtime AOT compile flags](feedback_wasmtime_compile.md) — Requires --wasm gc/function-references/exceptions flags for Kotlin WASM output
- [Feedback: Gradle build directory](feedback_gradle_build_dir.md) — run builds from skiko/skiko/, not skiko/
- [Feedback: wasmWasi reallocAllocator pollution](feedback_wasi_realloc_allocator.md) — first WASI call pollutes reallocAllocator; add freeAll at start of every WIT import
- [Feedback: cabi_realloc blocks host→guest record-with-strings](feedback_wasi_cabi_realloc_export_block.md) — wasmtime's typed export lowering throws "Can't create new allocators…" when passing records with string fields to a guest @WasmExport. Workaround: keep host→guest exports to primitive params only (task 49 step 1b shipped with `on-editor-attached(input-type)` no record). End-of-frame freeAll didn't fix it; root pollution source still unidentified. Revisit later.
- [Feedback: currentNanoTime traps WIT imports](feedback_currentnanotime_pollutes.md) — avoid `currentNanoTime()` near WIT imports; freeAll() does NOT clear its pollution
- [Feedback: PathBuilder shim must mirror mutatePath calls](feedback_pathbuilder_recursion.md) — missing PathBuilder method → unqualified call in compose-ui SkiaBackedPath mutatePath{} falls through to outer receiver → infinite recursion → SIGSEGV in wasmtime engine_type_index
- [Feedback: Compose interactivity broken at child-layer drawLayer](feedback_compose_interactivity.md) — input/state/recompose/layout/modifier-update/invalidate ALL work, but child OwnedLayers' drawLayer never invoked so pictures stay at records=1 and screen never updates
- [Feedback: WasiLifecycle → LocalLifecycleOwner bridge](feedback_lifecycle_owner_bridge.md) — pattern for providing a working androidx.lifecycle.LifecycleOwner on wasmWasi, fed by host activity-lifecycle events
- [Feedback: Granular lifecycle via Focused proxy](feedback_lifecycle_focus_proxy.md) — winit drops Activity onPause/onResume; use WindowEvent::Focused(bool) as proxy. Created/Started/Destroyed need future JNI hookup.
- [Feedback: Warm-resume preserves wasm state](feedback_warm_resume.md) — keep Store alive across suspend; swap renderer + inherit CPU caches (pictures/recorders/text-blobs/typefaces/paragraphs). Don't call eglTerminate in EglContext drop.
- [Feedback: RenderNode uses WasiDrawable](feedback_compose_render_node_picture.md) — C++ SkDrawable subclass with swappable inner from finishRecordingAsDrawable preserves deferred lookup; force-re-record workaround removed.
- [Feedback: RenderNode.drawInto attributes](feedback_render_node_attributes.md) — bounds/translation/pivot-aware-transform/clip/alpha+layerPaint via saveLayer/coarse-shadow all applied. setClipRect/RRect/Path no longer no-ops.
- [Feedback: Paint alpha pipeline](feedback_paint_alpha_pipeline.md) — witAttrs() must multiply color.alpha × paint.alpha into one effective byte; skia's SkPaint has ONE alpha, so independent fields desynced.
- [Feedback: identityHashCode wasi actual MUST use stable hashCode](feedback_transition_animate_to_bug.md) — RESOLVED 2026-05-13. Compose-runtime-wasi's Identity.hashCode() was reading a mutating global counter → DerivedSnapshotState validity check cached stale values → Material3 Checkbox/DropdownMenu Transition.animateTo froze after first cycle. Fix: bucket by target.hashCode() (stable), disambiguate equality with `===`.
- [Feedback: Compose popup overlay on wasi](feedback_popup_overlay.md) — Setup: CanvasLayersComposeScene + real WindowInfo.containerSize. Animated-graphicsLayer-invisible-in-popup bug RESOLVED 2026-05-20: layer alpha was baked into the parent recording (frozen at 0 for once-recorded popups); fixed in RenderNode.wasi.kt. No LocalInspectionMode workaround needed.
- [Feedback: Compose scrolling on wasi](feedback_scrolling.md) — Two fixes for scrollable: (A) flush WasiFrameDispatcher after every pointer event so scrollable drag-detect coroutine re-registers an awaiter; (B) was: invalidateParentLayer hack in GraphicsLayerOwnerLayer.move() — SUPERSEDED by host-side transforms below.
- [Feedback: host-side WasiDrawable transforms](feedback_host_side_transforms.md) — Compose layer position/translation/scale/rotation/clip/alpha/shadow live on the host's C++ WasiDrawable struct (Android-style live properties), set via non-recordable WIT calls. Setting them never invalidates parent recordings.
- [Feedback: BasicTextField freezes on legacy API; use TextFieldState API + on-key-event-v2 for hardware typing](feedback_basictextfield_freeze.md) — Legacy `BasicTextField(value, onValueChange)` freezes on tap (100% CPU, synchronous onTap path). NEW `BasicTextField(state: TextFieldState, ...)` works. Cursor-blink delay() spin separately fixed via WasiFrameDispatcher.Delay. Hardware keyboard typing wired via new `on-key-event-v2(kind, code-point, key-id)` WIT call + WasiInput.setKeyHandler — `adb shell input keyevent KEYCODE_A` types into focused field. LazyColumn fine as-is. Material3 TextField wraps legacy — also freezes.
- [Feedback: continuous-animation wasm linear-memory leak](feedback_indeterminate_progress_leak.md) — Indeterminate ProgressIndicator / any `while(true){ withFrameNanos {} }` loop leaks ~0.4 MB/s in wasm linear memory (Kotlin/Wasm codegen retains continuation refs). `Store::gc()` reduces 5x but CPU cost grows over time — net worse for sustained sessions. Mitigation: use `progress = { 0.5f }` static widgets. Bump to 2.4.0-RC didn't help (~2.5 MB/s soak result).
- [Reference: Kotlin Gradle Plugin version bump procedure](feedback_kotlin_version_bump.md) — 13 version-pin sites across skiko + 11 wasi modules. `KotlinWasmWasiTargetDsl` is bundled in `kotlin-gradle-plugin`, not a separate artifact. Build order needed for republish. Rust host/APK/WIT untouched by Kotlin bumps.
- [Reference: IME / soft-keyboard options](feedback_ime_options.md) — Integration point is `PlatformContext.startInputMethod(PlatformTextInputMethodRequest)`. Four paths: (1) JNI to Android `InputMethodManager` (proper, ~1-2 days, supports Gboard/voice/emoji/CJK), (2) HW-keyboard only (current state), (3) in-canvas Compose keyboard (no JNI, ~1 week, English-only practical), (4) per-node `PlatformTextInputModifierNode` (integration aid, not a backend). Recommended: combine (3) as default + (1) as opt-in upgrade.
- [Feedback: in-canvas soft keyboard — fully working](feedback_softkeyboard.md) — WasiSoftKeyboard end-to-end OK. Cursor-stays-at-0 / re-tap / long-press all FIXED 2026-05-13 by routing skia.paragraph.Paragraph's getRectsForRange / getGlyphPositionAtCoordinate / getWordBoundary through new WIT funcs to host skia-safe. Long-press needs ≥500ms dwell (haptic stub not wired — no buzz cue). Note 2026-05-28: in-canvas keyboard *retired* by task 49 — IME now runs as separate process (war.ime.keyboard) with overlay SF surface.
- [Feedback: IME layout arbitration](feedback_ime_layout_arbitration.md) — war.ime.keyboard's pickLayout reconciles editor-type override (host-driven) vs user 🌐 cycle, over a built-in + plugin source split. Plugin shape constraint: per-plugin WIT package because two deps cannot share a `linker.instance(name)` entry.
- [Feedback: pointerInput(Unit) stale-closure trap](feedback_pointer_input_stale_closure.md) — `pointerInput(Unit) { detectTapGestures(onTap = { onClick() }) }` captures `onClick` once for the lifetime of the node. Recompositions that change `onClick` (e.g. soft-keyboard Shift flip) don't update the closure → wrong-case letters, sticky toggles. Wrap with `rememberUpdatedState` for any handler that varies.
- [Feedback: wasmWasi single-thread constraint is Kotlin, not WASI](feedback_wasi_threading.md) — WASI P2 has `wasi:thread/spawn` and wasmtime runs threaded guests; what's single-threaded here is the wasmWasi Kotlin/Wasm runtime + kotlinx.coroutines on it. Frame concurrency bugs as "racing on the lone Kotlin dispatcher thread", not "single-threaded wasi".
- [Feedback: rsbinder @nullable callback workaround](feedback_rsbinder_nullable_callback.md) — rsbinder-aidl 0.7.0 doesn't translate AIDL @nullable to Option<&Strong>. Older HALs (Pixel 2 XL: getCapabilities=196, CAP_*_CALLBACK=0) refuse non-null callbacks with EX_UNSUPPORTED. Fix: bypass generated proxy, build parcel by hand with Option::<Strong>::None. Also: rsbinder needs android_11_plus (not android_11) feature for SDK 35 dispatch.
- [Feedback: rsbinder-aidl recursive parcelable limitation](feedback_rsbinder_aidl_recursive.md) — rsbinder-aidl 0.7.0 emits Vec<Box<Self>> for recursive parcelables (e.g. AttributionSourceState.next) but lacks SerializeArray/DeserializeArray for Box<T>, so the build won't compile. Workarounds: empty stub, int[] substitute, hand-write parcel encoder.
- [Feedback: AAudio direct-binder gotchas](feedback_aaudio_gotchas.md) — five things that bit task 21 B5 when bypassing libaaudio: SHARING_MODE constants reversed (EXCLUSIVE=0, SHARED=1), registerClient mandatory, Pixel 2 XL MMAP only stereo, writeable=false lie (must mmap PROT_RW), write-then-start order required.
- [Feedback: Kotlin/Wasm suspendCoroutine state-machine leak](feedback_kotlin_wasm_suspendcoroutine_leak.md) — wart-leak-repro: ~9 MB/s WasmGC-heap growth, OOM in 6:37. REVISED 2026-05-18: not Kotlin codegen — wasmtime DRC scheduling. See wasmtime-drc-no-autoschedule.
- [Feedback: wasmtime DRC has no auto-sweep](feedback_wasmtime_drc_no_autoschedule.md) — DRC sweeps only on memory.grow failure → unbounded growth; mitigation periodic Store::gc. Upstream auto-GC fix PR #13422 tested 2026-05-21: fixes the leak but reintroduces ANR via GC-frequency overhead — not a clean fix.
- [Feedback: don't fix DRC first_fit alone](feedback_drc_first_fit_alone_backfires.md) — Tested 2026-05-18: O(log F) first_fit by itself slowed sweep +64–74% AND raised guest alloc rate +27%. Upstream load-bearing fix is auto-scheduling; allocator-side fixes alone backfire.
- [Feedback: rebuild compose-*-wasi after skiko](feedback_rebuild_compose_after_skiko.md) — Every skiko republish requires rebuilding all 11 compose-*-wasi fat klibs (BUILD-wasmWasi.md Step 5). Symptoms of skipping: subtle behavioral drift (e.g. tap-latency accumulation), NOT link errors. Quick freshness check via mtime comparison.
- [Feedback: don't delete an app's cache/ dir](feedback_dont_delete_app_cache_dir.md) — `rm -rf cache/` breaks the host's `cache/ui.cwasm` write → silent "test frame" fallback + no control socket; mimics a guest crash/ABI-skew. Refresh a cwasm by REINSTALL, not dir-delete. (Burned ~1h on task 71.)
- [Reference: WASI component guest-language support](../../docs/wasm-component-language-support.md) — language-neutrality survey (docs/, not memory): Rust/C#/TinyGo first-class; Zig/Java manual/fork; Python/JS embed a VM; Java has NO WasmGC+WASI; Kotlin/Wasm is the only JVM-family WasmGC+WASI.
- [Feedback: compose-*-wasi dirs are bundle projects, not source](feedback_compose_wasi_srcdirs.md) — Edit Compose source in /home/harry/wart/compose-multiplatform-core/ (the only place source lives). compose-*-wasi dirs are gradle bundlers that srcDirs into core; their src/ holds only wasmWasiActuals + commonReplacements overrides.
- [Feedback: prefer wart-app edits over compose-multiplatform-core](feedback_prefer_wart_app_edits.md) — For tests/repros/instrumentation, default to wart-app/ changes. Only edit compose-multiplatform-core/ when no wart-app alternative exists, and ASK FIRST.
- [Feedback: compose-*-wasi/ is out of scope, will be deleted](feedback_compose_wasi_out_of_scope.md) — Don't develop new files/patterns in compose-*-wasi/. Wasi-specific overrides live in compose-multiplatform-core/.../wasmWasiMain/.
- [Feedback: worker-thread for Store backfires](feedback_worker_thread_backfires.md) — Tested 2026-05-18 in task 26: works on cold start, eliminates ANR, but introduces 5-6 s input-lag accumulation over minutes. ANR is the better failure mode for our workload. Don't redo without re-reading the task doc.
- [Feedback: don't no-op skiko Canvas stubs](feedback_canvas_stub_noop_traps.md) — Tempting to convert the throwing `Canvas.X: not implemented` stubs in SkiaTypes.wasi.kt to silent no-ops to unblock DatePicker/SegmentedButton. Don't — cold start passes, but Compose's save/restore invariants then fail in JIT'd wasm with SIGILL ~10-30 s into interaction. Keep stubs throwing; disable affected widgets.
- [Feedback: Material3 TooltipBox SIGILLs on wasi](feedback_tooltip_sigill_wasi.md) — Bisected end of task 28: `TooltipBox(PlainTooltip + rememberTooltipState)` triggers a deterministic ~5 s SIGILL after tap that wasmtime's handler can't intercept. **SUPERSEDED 2026-05-19** — true cause is [[wasi-adapter-state-corruption]] (adapter State.magic1 clobber), not a TooltipBox-specific bug.
- [Feedback: wasi adapter State corruption](feedback_wasi_adapter_state_corruption.md) — Task 30 Step 3: Tooltip SIGILL is adapter `State.magic1` clobbered at linear-mem 0x10008 by a Kotlin class-header word. RESOLVED 2026-05-21 (task 34 Option B fixed partition); self-heal workaround removed.
- [Reference: wasmtime 44 wasm-debugging APIs](reference_wasmtime_debug_apis.md) — call_hook, debug_exit_frames, breakpoints, single-step, debug_memory/global/function. Three tiers of precision (host-call / function-entry / instruction) with cost tradeoffs and the single-step gating recipe for nailing one-shot memory corruption.
- [Reference: wart wasi-tls transport (Signal de-risk)](reference_wart_wasi_tls_transport.md) — a wasm32-wasip2 guest reaches a TLS server with NO crypto in the guest via host-delegated wasi:sockets + wasi:tls (shipping in wasmtime 45); example.com → 200 OK. wart host must grant network (`inherit_network` + `allow_ip_name_lookup`; default deny-all). Signal fails only on cert pinning (`UnknownIssuer`) — fix is a custom `TlsProvider` host-side adding Signal's CA. Probe: `repros/wasi-tls-probe/`. Now wired into wart-host (task 66) + on-device-proven.
- [Project: Signal client architecture](project_signal_client_architecture.md) — task 67 (text-only Signal). Client is a wasm32-wasip2 GUEST using generic task-66 wasi:tls; host stays generic. Native-host-daemon design REJECTED (app logic belongs in guest). v1 foreground-only. Phase 0 gate: does libsignal/libsignal-service-rs build for wasm32-wasip2 + run sockets over wasi:tls? tasks/67.
- [Project: wart-step-executor](project_wart_step_executor.md) — persistent frame-stepped wasi:io/poll reactor so a guest's async tasks survive ACROSS component-export calls (wstd::block_on can't — its reactor dies per call, step methods pub(crate)). init/spawn/step/sleep/AsyncPollable; non-blocking step()=0-duration-poll trick. Signal engine (task 67 P2 item1) exports wart:signal/chat over it; desktop-verified link-url QR across poll-events. spawn() Task cancels on drop → .detach().
- [Feedback: wasi-adapter State corruption / KT-86415](feedback_kotlin_wasm_scopedmemory_destroy_bug.md) — UAF RESOLVED 2026-05-21 (task 34 Option B): fixed linear-memory partition — stdlib 2.4.258 root allocator at 0x20000 + adapter State::new pins State at [0x10000,0x20000). Device-verified, no regression vs baseline.
- [Feedback: Kotlin/Wasm println throws under wasmtime CLI](feedback_kotlin_wasm_println_throws_wasmtime.md) — wasmtime run with command-adapter: empty fun main() exits clean, but any meaningful body (println / @WasmImport call) throws "thrown Wasm exception" before main() runs. Composed component validates structurally. Use @WasmImport host-log pattern (skiko: WitCanvas.Import.logMessage) instead of stdio.
- [Reference: Kotlin/Wasm component-model status](reference_kotlin_wasm_component_model_status.md) — Kotlin 2.4's "Component Model support" is NOT native; it's the same embed + component-new --adapt(preview1 reactor) flow we use (verified via sample-wasi-http-kotlin Makefile). No escape from adapter/KT-86415/freeAll. WATCH KT-64568 (WASI Preview 2 target switch, still Planned) — the only path that retires the adapter. Our 2.4.255-SNAPSHOT has no new component-model codegen. Don't re-chase.
- [Feedback: wit-bindgen 0.53.1 has no Kotlin generator](feedback_wit_bindgen_no_kotlin_generator.md) — Upstream wit-bindgen-cli lists rust/c/cpp/go/csharp/moonbit/markdown — no `kotlin`. skiko's "Generated by wit-bindgen 0.53.1" header is misleading (private JetBrains fork). Hand-write following skiko's pattern; cabi_realloc via componentModelRealloc.
- [Feedback: canonical-ABI import/export asymmetry](feedback_canonical_abi_import_export_asymmetry.md) — Same WIT fn lowers differently per side: export `(I32 I32) -> I32` (callee-allocated return area); import `(I32 I32 I32) -> ()` (caller-allocated, return-area pointer as 3rd arg). wasmtime's Linker bridges. When hand-writing @WasmImport for rich return types, ALWAYS prepend a return-area pointer + drop the return.
- [Project: task 36 step 7 pending](project_task_36_step_7_pending.md) — RESOLVED 2026-05-26. Full cross-app dep chain runtime-validated end-to-end on device. New: `wart-host --run-once`, `LoadedApp::instantiate_command`, `md-smoke-rust/` (Rust consumer sidesteps Kotlin/Wasm command-adapter bug). Kotlin smoke proves install/load/linker; Rust smoke proves the call-through-proxy. `scripts/smoke-markdown.sh` is the dev pipeline.
- [Project: task 38 warpkg assets](project_task_38_warpkg_assets.md) — DONE 2026-05-26. Warpkg now ships bundled data files via top-level `assets/`. Installer auto-copies; runtime exposes via custom `my:skiko-gfx/assets.read` WIT verb (because Kotlin/Wasm 2.4 stdlib has no fd_read/path_open wrappers). WASI fs preopen at `/assets` also wired for future use. MarkdownCard reads `assets/demo.md` end-to-end.
- [Feedback: Rust component as CLI smoke pattern](feedback_rust_component_as_cli_smoke.md) — For one-shot wasi:cli/command WASM consumers (host plumbing smokes), write the consumer in Rust on wasm32-wasip2. Kotlin/Wasm + command adapter throws at module init unconditionally; Rust sidesteps it cleanly. Template lives at `md-smoke-rust/`. See [[kotlin-wasm-println-throws-under-wasmtime]].
- [Feedback: adb push dir nesting gotcha](feedback_adb_push_dir_nesting.md) — `adb push <src-dir> <dest-dir>` when dest exists nests source INSIDE: stale top-level files silently win for any consumer of that path. `rm -rf` dest before re-pushing warpkg dirs. Bit task 36 visuals v2 — caught only when md5sum diverged.
- [Feedback: Compose Row + weight + fillMaxWidth pattern](feedback_compose_row_weight_pattern.md) — For prefix+wrap-content layouts (bullet, ordered list, block-quote with vertical bar): Row needs `fillMaxWidth()` AND inner content Column needs `weight(1f)`. Neither alone works — get word-per-line wrap. Bit task 38 MarkdownCard.
- [Project: Signal wasip2 transport swap](project_signal_wasip2_transport_swap.md) — task 67 fork-and-swap: libsignal crypto compiles to wasm32-wasip2 (needs protoc>=3.12 + curve25519 [patch]); only reqwest transport blocks. Plan: shim reqwest API (~12 methods, http1-only) over wasi:tls + wstd executor in external/libsignal-service-rs/src/transport/. repros/signal-phase0/.
